# api Module

> **Living document.** Read this before modifying the module. Update it in the same change whenever the module's behavior, endpoints, files, or dependencies change.

**Source:** `api/` · **Last verified:** 2026-08-02 (final fix wave — `POST /rpc`'s unexpected-error branch no longer echoes `error.message` to callers, see `bootnode/routes.ts`'s entry below; CT retention-expiry doc note added, see Gotchas)

## Purpose

`@grantfox/api` — the Postgres-backed indexer package for the privacy
wallet. Task 5 shipped the schema, the drizzle client factory, and the
`IndexerRepo` data-access surface. Task 6 added the two event sources:
`fetchRpcEvents` (Soroban RPC `getEvents`) and `fetchBootnodeEvents` (the
SPP bootnode's cached `getEvents`, used to bootstrap history past the RPC's
retention window until it hands off back to the RPC) — both return the
same `EventsPage` shape. Task 7 adds the polling engine
(`api/src/modules/indexer/poller.ts`: `pollStream`/`pollStreams` + the two
`StreamSource` factories) and the worker process (`api/src/worker.ts`) that
drives it on an interval, with per-stream exponential backoff and isolation
and a graceful-shutdown loop. Task 8 adds the CT normalizer
(`api/src/modules/indexer/normalize-ct.ts`: `normalizeCtEvent`, pure) and
wires it into `pollStream` as a post-insert step inside the SAME
transaction as the raw-event insert, CT stream only (`ctTokenId` param) —
`worker.ts`'s CT stream state now carries `TESTNET.ct.token` so every CT
event the worker fetches also lands normalized `ct_activity` rows. No HTTP
server exists yet (Task 9).

**Task 8.5 (this update) — SPP historical backfill from an archival
source.** Both of Task 7's live SPP sources turned out to be permanently
dead for the SPP pool's actual history: the RPC's retention window starts
AFTER the pool's deployment ledger (see the "Live-verification blocker"
gotcha below), and Nethermind's public bootnode rejects every filter set
tried with `-32602 unsupported filters`. Task 8.5 recovers that history
from stellar.expert's public archive instead — `api/scripts/backfill-spp.ts`
fetches all 4 of the SDK's sync-set contracts' events (asc, complete to
tip), resolves each event's `txHash` via Horizon testnet, and writes a
deterministic, committed fixture (`api/fixtures/spp-backfill.json`).
`api/scripts/load-backfill.ts` loads that fixture into `events`
(idempotent, on-conflict-do-nothing — same mechanism as the live pollers)
and initializes the worker's SPP stream cursor straight to RPC mode, so the
worker's very next poll for that stream skips the dead bootnode entirely.
Backfilled rows are produced by `api/src/lib/spp-archive.ts`'s
`mapArchiveRecordToRawEvent`, which REUSES `soroban-events.ts`'s
`naturalEventId`/`eventIndexFromId` so a row is byte-identical in shape
(same `events.id` format, same base64-XDR topic/value representation)
whether it came from stellar.expert, the RPC, or (hypothetically) the
bootnode.

**Task 9 — API server (REST + SPP bootnode-protocol endpoint).** Adds the
first (and only) HTTP surface this package exposes: a Fastify app
(`api/src/app.ts`, listened on by `api/src/server.ts`) serving two things.
(1) REST routes (`modules/activity/routes.ts`) mirroring `@ctd/indexer`'s
handler API — `GET /health`, `GET /contracts/:contractId/events`
(startLedger/endLedger/cursor/limit, default 200/max 1000, base64-of-last-id
cursor, oldest→newest) — plus this repo's own `GET /accounts/:address/activity`
(newest-first, same cursor convention, reading `ct_activity`). (2) a
bootnode-protocol JSON-RPC endpoint (`modules/bootnode/`) at `POST /rpc` —
`getLatestLedger` (a live proxy to the upstream RPC) and `getEvents` (served
straight from `events`, no caching layer — see Gotchas for why this
deliberately diverges from the reference bootnode's stateful
cache/tip-tracking design). This is now the ONLY live source of SPP pool
history anywhere reachable (Nethermind's own public bootnode is dead — see
the "Live-verification blocker" gotcha below — and Task 8.5's archival
recovery is what backfilled `events` in the first place). Closed two carried
gaps as a prerequisite: `worker.ts`'s SPP stream and the bootnode's
allow-list now both track the SDK's FULL 4-contract sync set (previously
only 2 of 4 were polled live — see the `buildSppContractIds` gotcha below),
and `packages/shared/src/config.ts` gained `TESTNET.spp.poolEurc`
(previously hardcoded only in `backfill-spp.ts`). New query methods on
`IndexerRepo` (`listEventsFromLedger`/`listEventsAfterId`/`getLedgerBounds`/
`listActivityForAccount`/`listActivityForAccountBeforeId`) back both the
REST and bootnode read paths; `bootnode_pages` (Task 5) is left UNUSED —
serving live from `events` was simpler and the brief explicitly didn't
mandate speculative caching.

**Task 11 — CORS.** The browser wallet (`@grantfox/app`) is the first
consumer that calls this API directly from a different origin (dev:
`http://localhost:5173`; a deployed static-site origin in production) rather
than server-to-server/CLI — every prior Task 9 live-verification curl'd from
the same host. Without CORS, the browser silently blocks the response and
`app/src/lib/ct-indexer.ts`'s `fetchEvents`/`resolveEventRef` and
`app/src/components/ActivityFeed.tsx`'s activity fetch both fail with a
generic `Failed to fetch`. Fixed by registering `@fastify/cors` (`^11.3.0`,
new dependency) in `buildApp` (`app.ts`) with `{ origin: true }` (reflects
the request's own `Origin`) — permissive because every route here is an
unauthenticated read of public, already-on-chain-derived data; there is no
session/cookie/secret a stricter allow-list would protect. Verified live: a
`curl -H "Origin: http://localhost:5173"` against `/accounts/:address/activity`
returns `access-control-allow-origin: http://localhost:5173`; the full
Playwright browser e2e run (`docs/modules/app.md`'s Task 11 entry) confirms
the wallet's own cross-origin fetches succeed end-to-end.

## ⚠️ Required migration step when deploying Task 9's worker change

**Any deployment that ran the worker BEFORE this task's commit** (i.e., has
a `cursors` row for the OLD 2-contract SPP stream key) **must re-run the
backfill loader once, after deploying, before starting the new worker.**
Skipping this silently stalls SPP ingestion — no crash, just a quiet
per-tick failure loop.

**Why**: Task 9 changed the SPP stream's tracked contract set from 2
contracts (`pool`, `publicKeyRegistry`) to the SDK's full 4-contract sync
set (`pool`, `poolEurc`, `aspMembership`, `publicKeyRegistry` —
`worker.ts`'s `buildSppContractIds()`). The `cursors` table row the SPP
stream reads/writes is keyed by `` `spp:${contractIds.join(",")}` ``
(`buildSppStreamKey()`) — changing the contract set changes this key. An
existing deployment's `cursors` table still has a row under the OLD key;
there is no row under the NEW key.

**Failure mode if skipped**: the worker's next `tick()` calls
`repo.getCursor(newKey)`, gets `null` (the new key has never been written —
the old key's row is just orphaned, still sitting there unused), and
`makeBootnodeThenRpcSource` treats a `null` cursor as "first poll, start in
bootnode mode" (see `poller.ts`'s module doc). It calls Nethermind's public
bootnode, which is dead — every request gets `-32602 unsupported filters`
(see the "Live-verification blocker" gotcha below). The stream fails every
tick, backs off up to the 60s cap, and SPP ingestion silently stops forever
— the CT stream is unaffected (per-stream isolation), so nothing else looks
broken, and the only symptom is a repeating `[worker] stream "spp:..."
failed` line in logs.

**Required action**:
```
DATABASE_URL=... pnpm --filter @grantfox/api backfill:spp:load
```
This runs `scripts/load-backfill.ts`, which calls the real, idempotent
`initSppCursor(repo, SPP_STREAM_KEY, lastBackfilledLedger)` —
`SPP_STREAM_KEY` now resolves to the NEW 4-contract key (it imports
`buildSppStreamKey()`, see the Gotchas entry below) — writing that key's
cursor straight to RPC mode (`rpc:ledger:<lastBackfilledLedger + 1>`). The
worker's next poll for the SPP stream then goes straight to the RPC,
skipping the dead bootnode entirely, exactly as the original Task 8.5
backfill did for the old key. Safe to re-run on an already-migrated
deployment: `insertEvents` is on-conflict-do-nothing and `initSppCursor`
never regresses an already-`rpc:`-mode cursor (verified by
`spp-backfill-loader.test.ts`'s "does NOT overwrite an existing rpc:-mode
cursor" case).

A brand-new deployment (empty `cursors` table, no prior worker run) does
NOT need this as a separate step — running `backfill:spp:load` once, as
already documented in Task 8.5's Testing section below, is sufficient;
there is no old key to orphan.

## Structure

| File | Purpose |
|---|---|
| `api/src/db/schema.ts` | drizzle `pgTable` definitions for the four tables: `events`, `ct_activity`, `cursors`, `bootnode_pages`. Exports `*Row`/`New*Row` inferred types per table. |
| `api/src/db/client.ts` | `createDb(url): { db, pool }` — builds a `pg.Pool` + `drizzle-orm/node-postgres` handle bound to `schema`. Caller owns `pool.end()`. |
| `api/src/db/repo.ts` | `createRepo(db): IndexerRepo` — the data-access surface: batch `insertEvents` (on-conflict-do-nothing by `id`), `getCursor`/`setCursor`, `insertCtActivity`, `insertBootnodePage`/`getBootnodePage`, `withTransaction(fn)` (re-scopes all of the above to a single Postgres transaction), and (Task 9) five read-only query methods backing the API server: `listEventsFromLedger`/`listEventsAfterId` (events for a contract set, ordered by the REAL chain-position tuple `(ledger, txIndex, opIndex, eventIndex)` — NOT string `id` order, see Gotchas), `getLedgerBounds` (`{min,max}` ledger, scoped or global), `listActivityForAccount`/`listActivityForAccountBeforeId` (`ct_activity`, newest-first, keyset on `(ledger, id)`). |
| `api/src/lib/env.ts` | `loadEnv(source?)` — zod-validated process env: `DATABASE_URL` (required), `POLL_INTERVAL_MS` (optional, default `5000`), `RPC_URL`/`BOOTNODE_URL` (optional, default to `TESTNET.rpcUrl`/`TESTNET.spp.nethermindBootnode` — must stay UNSET to take the default; an empty string fails validation, see Gotchas), `PORT` (Task 9, optional, default `3000` — the API server's listen port; the server also reuses `RPC_URL` as its upstream-proxy target, no separate var). |
| `api/src/index.ts` | Public package entry point; re-exports the Task 5 surface (`createDb`/`createRepo`/schema/`loadEnv`). Does NOT re-export the Task 7 poller/worker or the Task 9 app/server — nothing outside `api/` consumes them as a library; `worker.ts`/`server.ts` are standalone entrypoints. |
| `api/src/app.ts` (Task 9; CORS added Task 11) | `buildApp(deps: AppDeps): FastifyInstance` — assembles both route sets (`modules/activity`, `modules/bootnode`) onto one Fastify instance; `AppDeps.repo` is typed as the read-only intersection the routes actually use (`ActivityRepoDeps & BootnodeRepoDeps`), not the full `IndexerRepo` (this server never writes). Separated from `server.ts`'s `listen()` so tests can `app.inject()` without binding a port. Registers `@fastify/cors` (`{ origin: true }`) FIRST, ahead of the route registrations, so every response carries `access-control-allow-origin` — the browser wallet's cross-origin reads (see "Task 11 — CORS" above). |
| `api/src/app.test.ts` (Task 9) | Smoke test: both route sets are reachable on one instance (`/health` 200, `POST /rpc` dispatches to `handler.ts`) — the individual route/handler logic is covered in depth by the three test files below. |
| `api/src/server.ts` (Task 9) | Process entrypoint: `loadEnv`, `createDb`/`createRepo`, `buildApp({repo, allowedContractIds: buildSppContractIds(), rpcUrl: env.RPC_URL})`, `app.listen({port: env.PORT, host: "0.0.0.0"})`, clean shutdown on SIGINT/SIGTERM (`app.close()` then `pool.end()`, mirroring `worker.ts`'s `main()` ordering). Same `isMainModule` ESM entrypoint guard as `worker.ts`. Run via `pnpm --filter @grantfox/api dev` (tsx) or `pnpm --filter @grantfox/api start` (built, `node dist/server.js`). |
| `api/src/modules/activity/routes.ts` (Task 9) | `registerActivityRoutes(app, repo: ActivityRepoDeps)` — `GET /health` (`{latest_synced_ledger}` from unscoped `getLedgerBounds().max`), `GET /contracts/:contractId/events` (`{latestLedger, cursor, events:[{id,ledger,txHash,topic,value}]}`, limit default 200/max 1000, base64-of-last-id cursor, `limit+1` over-fetch to detect a further page — mirrors `@ctd/indexer`'s `events.ts`/`queries.ts` field-for-field, see the file's module doc for the one place parity does NOT extend to: `IndexerClient`'s Goldsky-JSON content decoder), `GET /accounts/:address/activity` (`{cursor, activity:[...]}`, newest-first, same cursor convention — this repo's own design, no external reference). |
| `api/src/modules/activity/routes.test.ts` (Task 9) | `fastify.inject` against a fake `ActivityRepoDeps` (offline): response-shape parity assertions for `/health`/`/contracts/:id/events` against the reference's documented field names/defaults, cursor round-trip (base64 encode on the way out, decode on the way in), `limit+1` over-fetch wiring, 400 `INVALID_ARGUMENT` on a non-integer query param and on an unresolvable cursor. |
| `api/src/modules/bootnode/handler.ts` (Task 9) | `handleGetEvents(rawParams, deps): Promise<RpcOutcome<GetEventsResult>>` / `handleGetLatestLedger(deps): Promise<RpcOutcome<GetLatestLedgerResult>>` — the bootnode-protocol business logic, transport-agnostic (returns a typed `{ok:true,result}\|{ok:false,error}`, never throws for an EXPECTED JSON-RPC error case). Validation order (replicated from `rpc.rs`, see the file's module doc): params shape (zod `.strict()`, `deny_unknown_fields`-equivalent) → `endLedger` rejected → `xdrFormat` rejected → exactly one of `startLedger`/`pagination.cursor` → `isAllowedFilters` (exactly one filter, `type:"contract"`, `topics:[["**"]]`, `contractIds` set-equal to `deps.allowedContractIds`, order-independent). Handoff/cache-miss semantics are a DELIBERATE simplification of the reference's stateful cache — see the file's module doc for the full rationale and the Gotchas entry below. Exports the JSON-RPC error codes (`RETENTION_HANDOFF_CODE -32002`, `CACHE_MISS_CODE -32004`, `INVALID_PARAMS_CODE -32602`, `INTERNAL_ERROR_CODE -32603`) and the wire types (`WireEvent`, `GetEventsResult`, `GetLatestLedgerResult` — field names byte-for-byte matching `messages.rs`'s `Event`/`GetEventsResponse`, see Gotchas). |
| `api/src/modules/bootnode/handler.test.ts` (Task 9) | TDD spec (written before the implementation) covering the brief's required cases — valid page (both `startLedger` and `cursor` forms), both-params error, unsupported-filter error (wrong type/topics/contractIds/count, order-independent contractIds match), handoff (both forms, and the zero-data edge case), cache miss (unresolvable cursor, cursor for a non-allowed contract) — plus the validation-order edges (`endLedger`/`xdrFormat` checked before filters even when filters are also invalid) and `getLatestLedger`. Offline: an in-memory `FakeBootnodeRepo` REPLICATING the real tuple-ordering/cursor-resolution semantics (not a stub), `fetchLatestLedger` injected. |
| `api/src/modules/bootnode/routes.ts` (Task 9) | `registerBootnodeRoutes(app, deps: BootnodeHandlerDeps)` — `POST /rpc`, the JSON-RPC 2.0 transport: dispatches `getEvents`/`getLatestLedger` to `handler.ts`, wraps its `RpcOutcome` in `{jsonrpc,id,result}`/`{jsonrpc,id,error}`. Owns what `handler.ts` doesn't: unknown method → `-32601`, malformed body (no `method` string) → `-32600`, and a try/catch around dispatch mapping any unexpected thrown error (DB/network failure) → `-32603` rather than a raw 500 — **the wire `message` is always the literal string `"internal error"` (review fix, final fix wave)**, never `error.message`, since this is a publicly-advertised endpoint and the real error (pg/undici internals, stack trace) could leak implementation details; `request.log.error` still logs the real error server-side. Batch requests unsupported (matches the reference's `BatchRequestConfig::Disabled`). Errors are HTTP 200 (JSON-RPC convention, matches `bootnode-client.ts`'s own parsing, which never checks `res.ok`). |
| `api/src/modules/bootnode/routes.test.ts` (Task 9) | `fastify.inject` against a fake `handler.ts` deps object: method dispatch + `id` echo for both methods, the `-32600`/`-32601`/`-32603` transport-level cases, `id: null` when omitted. |
| `api/src/lib/rpc-proxy.ts` (Task 9) | `fetchUpstreamLatestLedger(rpcUrl, opts?): Promise<UpstreamLatestLedger>` (`{id, protocolVersion, sequence, closeTime}`) — a raw JSON-RPC 2.0 POST to the upstream Soroban RPC, deliberately NOT going through `@stellar/stellar-sdk`'s `rpc.Server.getLatestLedger()` (whose parsed `Api.GetLatestLedgerResponse` types `protocolVersion` as a `string`, but the real wire response — and the Rust bootnode-protocol structs' `u32` field — need it as a JSON number; verified live against `soroban-testnet.stellar.org`, see the file's module doc). Same "pure adapter, injectable `fetch`, `AbortController`+`setTimeout`-bound" convention as `bootnode-client.ts`. Backs both `getLatestLedger` and `getEvents`'s `latestLedger`/`latestLedgerCloseTime` fields. |
| `api/src/lib/rpc-proxy.test.ts` (Task 9) | Offline: mocks global `fetch`; wire-shape assertion, extra-field stripping (`headerXdr`/`metadataXdr` dropped), JSON-RPC error mapping, non-OK HTTP mapping, malformed-field rejection, timeout. |
| `api/src/modules/indexer/poller.ts` | The polling engine. `pollStream(source, repo, streamKey, ctTokenId?): Promise<void>` — one page per call: fetch (network I/O, outside any transaction), then `repo.withTransaction` batch-inserts events, then (Task 8, only when `ctTokenId` is passed) runs each fetched event through `normalizeCtEvent` **inside a per-event try/catch** (review fix, see Gotchas) and `insertCtActivity`s the surviving rows — all as one post-insert step INSIDE the same transaction — then advances the cursor, all as one unit. `pollStreams(streams, repo, shouldStop?)` — runs `pollStream` per stream (forwarding each `StreamConfig`'s optional `ctTokenId`) with per-stream try/catch isolation, returning `{streamKey, ok, error?}[]`; `shouldStop` (optional) is checked between iterations (never mid-flight) so a shutdown signal can skip streams not yet started (review fix, see Gotchas). `StreamSource` — the fetch abstraction (`fetchPage(cursor): Promise<{events, nextCursor}>`); `makeRpcSource(config)` (pure-RPC, the CT stream) and `makeBootnodeThenRpcSource(config)` (bootnode-until-handoff-then-RPC, the SPP stream) are the two concrete factories, both taking an optional `timeoutMs` (review fix) and each injectable with a fake `fetchEvents`/`fetchBootnode` for tests. Owns the cursor-value encoding (`ledger:<N>`, `handoff:<N>`, `rpc:<...>`) documented in the file's module doc comment. |
| `api/src/modules/indexer/poller.test.ts` | TDD spec for the five invariants (batch+cursor atomicity; cursor advances past a filtered page; resume-from-cursor with no duplicate `events.id`; bootnode handoff → RPC switch, restart-safe; per-stream error isolation) plus `makeRpcSource` unit cases, a `pollStreams`/`shouldStop` early-exit case, and (Task 8) a `describe` block proving the CT-normalization wiring: passing `ctTokenId` normalizes fetched events into `ct_activity` rows in the SAME transaction; a non-CT-activity event still inserts its raw row but no `ct_activity` row; omitting `ctTokenId` (the SPP call site) never touches `ct_activity` even for a normalizable event; a mid-transaction crash rolls back the `ct_activity` insert too, not just the cursor; and (review fix) a page with ONE poisoned event (a valid mapped topic but a value XDR `normalizeCtEvent` throws on) still inserts BOTH raw events, still inserts the good event's `ct_activity` row, still advances the cursor, and logs the failure with the offending event's id (asserted via a `console.error` spy). Offline via an in-memory `FakeRepo` with REAL stage/commit/rollback transaction semantics (not a stub) for invariant 1's and the CT-wiring rollback case's proof; two `describe.skipIf(!DATABASE_URL)` blocks re-prove invariant 1 and the CT wiring against real Postgres. |
| `api/src/modules/indexer/normalize-ct.ts` | The CT normalizer (Task 8). `normalizeCtEvent(raw: RawEvent, tokenId: string): CtActivityInsert[] \| null` — pure (no I/O, no `Date.now()`): returns `null` if `raw.contractId !== tokenId`, if `raw.topic` is empty, or if `topic[0]`'s symbol isn't one of `register`/`deposit`/`merge`/`withdraw`/`transfer`. Otherwise decodes the topics/value XDR (`xdr.ScVal.fromXDR` + `Address.fromScVal`/`scValToNative`/`.bytes()`) and maps to `ct_activity` row(s): `register`/`merge` → one self-row, no amount, empty `ciphertexts`; `deposit` → a row per side (collapsed to one when `from === to`), public `amount`, empty `ciphertexts`; `withdraw` → one row for `from` only, public `amount`, `ciphertexts` = the 4 checkpoint fields (`rE`/`sigma`/`bTilde`/`bAudS`); `transfer` → a row per side (also collapsed to one when `from === to`, same rationale as `deposit`), `amount: null`, `ciphertexts` = the full 8-field set (adds `vTilde`/`vAudR`/`rAudR`/`vAudS`). Ciphertext values are `0x`-prefixed lowercase hex of the RAW on-chain `BytesN` bytes — not decoded through any curve-point math (see Gotchas: field names). **Deliberately fail-fast, not defensive** (review fix, see Gotchas): a malformed/missing value field throws rather than being swallowed — `poller.ts` owns per-event error isolation, not this function. `CtActivityInsert` is an exported alias for `NewCtActivityRow`. |
| `api/src/modules/indexer/normalize-ct.test.ts` | TDD spec against 13 REAL events fetched live from the deployed CT contract (`__fixtures__/ct-events.json`, committed; provenance and decode-transcript pointer in the file's module doc). Covers: non-CT-activity topics (the 4 constructor-setter events) → `null`; contractId mismatch → `null`; empty `topic` → `null`; a CT-emitted-but-unmapped event (`spender_transfer`, XDR round-trip-verified, not hand-encoded) → `null`; register (2 real events, one C-address one G-address account); deposit (self-deposit → 1 row; a synthesized cross-account variant → 2 rows, same amount); merge; transfer (2 rows, `amount: null`, all 8 hex ciphertext fields asserted against hand-decoded values, plus a synthesized self-transfer → 1 row, review fix); withdraw (1 row, public amount, 4 hex ciphertext fields); purity (no input mutation; deterministic same-input-twice); and (review fix) a fail-fast case — real `withdraw` topics paired with a real `register` event's `valueXdr` (missing the fields `withdraw` needs) asserts `normalizeCtEvent` THROWS rather than returning `null`/a partial row. |
| `api/src/modules/indexer/__fixtures__/ct-events.json` | Committed fixture: 13 real events from the deployed CT contract `CBTEJFLW25UXIDAIWJ3KUJGI5CE2YLHM5GQM2VFU7JQZS53HE3HKGCLH` (ledger 3900254–3900654), `RawEvent[]` JSON (dates as ISO strings, restored to `Date` by each consuming test's `loadFixtures`/`loadCtFixtures` helper). Shared by `normalize-ct.test.ts`, `poller.test.ts`'s CT-wiring cases, and `worker.test.ts`'s CT-forwarding case — one capture, three test files, so they can never disagree about what a real CT event looks like. |
| `api/src/worker.ts` | The worker process. `backoffDelayMs(consecutiveFailures)` — `1s * 2^(n-1)`, capped at 60s. `StreamRuntimeState`/`buildInitialStreamState(streamKey, source, ctTokenId?)` — per-stream `{consecutiveFailures, nextAttemptAt, ctTokenId?}` bookkeeping; `ctTokenId` (Task 8) is forwarded verbatim to `pollStreams`' `StreamConfig`. `tick(states, repo, now?, shouldStop?)` — polls every stream whose `nextAttemptAt <= now` via `pollStreams` (forwarding `shouldStop` and each state's `ctTokenId`), updates backoff state per outcome. (Task 9) `buildSppContractIds()` — the SDK's full 4-contract SPP sync set (`[pool, poolEurc, aspMembership, publicKeyRegistry]`), and `buildSppStreamKey(contractIds?)` — `` `spp:${contractIds.join(",")}` ``, defaulting to `buildSppContractIds()`; both exported so `spp-backfill-loader.ts` and the bootnode handler's allow-list share ONE source of truth instead of hand-replicating the array (see Gotchas — this closes a drift risk Task 8.5 explicitly flagged). `main()` — process entrypoint: `loadEnv`, `createDb`/`createRepo`, builds the CT (`makeRpcSource`, `ctTokenId: TESTNET.ct.token`) and SPP (`makeBootnodeThenRpcSource` over `buildSppContractIds()`, no `ctTokenId`) stream states, loops `tick` on `POLL_INTERVAL_MS` (passing `() => shuttingDown` as `shouldStop`) with `Promise.race` early-wake on SIGINT/SIGTERM, `pool.end()` on exit. Guarded by an ESM `import.meta.url === pathToFileURL(process.argv[1]).href` check so `main()` only auto-runs when this file is the process entrypoint (not when imported by tests or `spp-backfill-loader.ts`/`server.ts`). |
| `api/src/worker.test.ts` | Unit tests for `backoffDelayMs`'s table of inputs, `tick`'s scheduling (a backing-off stream is skipped until its window elapses; a healthy stream is unaffected; a recovered stream's backoff resets to zero), that `tick` forwards `shouldStop` to `pollStreams`, and (Task 8) that `tick` forwards a stream's `ctTokenId` through to `pollStream`'s CT normalization — a real register-event fixture fed to BOTH a CT-tagged and a plain SPP-tagged `StreamRuntimeState` in the same `tick` call produces a `ct_activity` row for only the CT one. Does not exercise `main()` — process lifecycle (signals, `pool.end()`) was verified by the bounded live run instead (see Testing). |
| `api/drizzle.config.ts` | `drizzle-kit` config — schema path, migrations `out: ./drizzle`, `dialect: postgresql`, reads `DATABASE_URL` (falls back to the local docker default). |
| `api/drizzle/0000_unusual_captain_marvel.sql` | First (and, as of this task, only) migration — `CREATE TABLE` for all four tables. |
| `api/src/db/schema.test.ts` | Offline: asserts each table's column names/types/PK against this doc (drizzle `getTableColumns`/`getTableName`), plus an `events.id` format check. |
| `api/src/lib/env.test.ts` | Offline: `loadEnv` required/default/coercion/failure cases. |
| `api/src/db/repo.test.ts` | Integration, `describe.skipIf(!process.env.DATABASE_URL)`: round-trips every `IndexerRepo` method against the local docker Postgres, including transaction commit/rollback. |
| `api/src/lib/soroban-events.ts` | `fetchRpcEvents(rpcUrl, {contractIds, startLedger?, cursor?, limit, timeoutMs?}): Promise<EventsPage>` — wraps `rpc.Server.getEvents` (sdk-16); `timeoutMs` (default `DEFAULT_FETCH_TIMEOUT_MS` = 30s, also exported) bounds the call via `server.httpClient.defaults.timeout` (review fix — see module doc for why the `RpcServer` constructor's own `timeout` option does NOT work in sdk-16.2.0). Also exports `EventsPage`, `RawEvent`, `naturalEventId`, `eventIndexFromId` (shared with `bootnode-client.ts`). Pure I/O adapter: no DB access, no retry/backoff. |
| `api/src/lib/bootnode-client.ts` | `fetchBootnodeEvents(url, {contractIds, startLedger?, cursor?, timeoutMs?}): Promise<EventsPage \| {handoff: {fromLedger: number}}>` — JSON-RPC 2.0 POST to the SPP bootnode's `getEvents`. Maps `-32002` (retention handoff) to a `{handoff}` return value, throws `BootnodeCacheMissError` on `-32004`. `timeoutMs` (default `DEFAULT_FETCH_TIMEOUT_MS`, imported from `soroban-events.ts`) bounds the `fetch` call via a plain `AbortController`+`setTimeout` (review fix). Pure I/O adapter, `fetch`-based, mocked in tests. |
| `api/src/lib/soroban-events.test.ts` | Offline: mocks `rpc.Server.prototype.getEvents` (`vi.spyOn`); valid-page mapping, cursor-vs-startLedger request shape, missing-`startLedger`/`cursor` guard, missing-`contractId` mapping error, `eventIndexFromId`/`naturalEventId` unit cases, and a `timeoutMs`-wiring case (captures `this.httpClient.defaults.timeout` inside a `mockImplementation`). |
| `api/src/lib/bootnode-client.test.ts` | Offline: mocks global `fetch` (`vi.stubGlobal`); wire-shape (`pagination.cursor`, `deny_unknown_fields`-safe body) assertions, valid-page mapping, `-32002` handoff mapping, both `-32004` message variants, an unmapped JSON-RPC error code, missing-required-field mapping error, missing-`startLedger`/`cursor` guard, and a timeout case (a `fetch` mock that only ever settles via the passed `AbortSignal`, asserting rejection within a 20ms configured timeout). |
| `api/src/lib/spp-archive.ts` (Task 8.5) | Pure decode/mapping logic for the SPP backfill, no I/O. `decomposeToid(toid: bigint): {ledger, txIndex, opIndex}` — Stellar TOID bit layout (`ledger(32)｜txOrder(20)｜opOrder(12)`), verified against a real live event (module doc has the full transcript). `txToidForEvent(eventToid)` — zeroes the low 12 (opOrder) bits, the transaction-level TOID Horizon's `/transactions` paging tokens actually use (refines the brief's literal `cursor=<toid-1>` recipe — see Gotchas). `toidFromArchiveId(id)` — the numeric TOID prefix of an archive record's `<toid>-<eventIndex>` id (the counterpart to `soroban-events.ts`'s `eventIndexFromId`, which extracts the suffix). `mapArchiveRecordToRawEvent(record, txHash): RawEvent` — REUSES `naturalEventId`/`eventIndexFromId` from `soroban-events.ts` so a backfilled row is byte-identical in shape to a live-RPC row. `compareArchiveOrder(a, b)` — deterministic sort (TOID, then eventIndex, then contractId) for the committed fixture. |
| `api/src/lib/spp-archive.test.ts` (Task 8.5) | TDD spec, all fixtures REAL data live-fetched from stellar.expert/Horizon (not hand-constructed) — the known-TOID-vector proof the brief requires, plus a real record→`RawEvent` mapping case, a same-TOID/different-eventIndex case, and `compareArchiveOrder` ordering/stability cases. |
| `api/src/lib/spp-archive-client.ts` (Task 8.5) | I/O adapters, same "pure adapter, injectable `fetch`" convention as `soroban-events.ts`/`bootnode-client.ts` — NOT unit tested against a live network (per the brief), exercised by actually running `backfill-spp.ts`. `fetchArchiveEventsPage(baseUrl, contractId, opts)` / `fetchAllArchiveEvents(...)` — paginate stellar.expert's `GET /explorer/testnet/contract/<id>/events`, oldest-first, `politeDelayMs` (default 300ms) between page requests. `resolveTxHash(horizonUrl, eventToid, cache, fetchImpl?)` — resolves a TOID's `txHash` via Horizon (`cursor=<txToidForEvent(eventToid)-1>&limit=1&order=asc`), caches by the tx-level TOID (many events share one tx), and asserts the returned transaction's own `paging_token` matches the expected tx-level TOID exactly before trusting it. `fetchJsonWithRetry` (internal) retries 429/5xx AND thrown network errors (DNS/connection-reset — added after a live run hit a mid-run `ECONNRESET`) using `backoffDelayMs` (imported from `worker.ts`, not re-implemented) up to 6 attempts. |
| `api/src/lib/spp-backfill-loader.ts` (Task 8.5, updated Task 9) | `SPP_STREAM_KEY = buildSppStreamKey()` — now IMPORTS `worker.ts`'s `buildSppStreamKey()` instead of hand-replicating the computation (Task 8.5 originally replicated it because `buildStreamStates` wasn't exported and flagged the drift risk explicitly; Task 9 closes it at the root — see `worker.ts`'s row above and the Gotchas entry). `initSppCursor(repo, streamKey, lastBackfilledLedger): Promise<SppCursorInitResult>` — writes `encodeRpcModeLedgerCursor(lastBackfilledLedger + 1)` (from `poller.ts`) IFF no cursor exists yet or the existing one is still bootnode-mode (`null`/bare token/`handoff:...`); never regresses an already-`rpc:`-mode cursor. `loadBackfillEvents(repo, rows, chunkSize?)` — chunked, idempotent (`insertEvents`' on-conflict-do-nothing) batch insert. |
| `api/src/lib/spp-backfill-loader.test.ts` (Task 8.5) | TDD spec against a minimal in-memory fake repo: `SPP_STREAM_KEY`'s literal value (pins it against undetected drift from `worker.ts`'s actual key), `initSppCursor`'s full decision table (no cursor / bare bootnode token / `handoff:` marker / already `rpc:`-mode, including a lower-ledger `rpc:ledger:` case that must NOT regress), and `loadBackfillEvents`'s chunking + idempotent-re-run + empty-fixture cases. |
| `api/scripts/backfill-spp.ts` (Task 8.5) | Entrypoint script (run via `tsx`, not part of `tsc` build — see Gotchas): fetches all 4 SPP contracts' full event history from stellar.expert, resolves every event's `txHash` via Horizon (cached per tx), maps + sorts deterministically, writes `api/fixtures/spp-backfill.json`. Idempotent re-runs refresh the fixture to on-chain tip at run time. |
| `api/scripts/load-backfill.ts` (Task 8.5) | Entrypoint script: reads the fixture (path arg optional, defaults to `api/fixtures/spp-backfill.json`), calls `loadBackfillEvents` + `initSppCursor` against a real `IndexerRepo`, then re-queries `events` per contract for the real post-load counts (not just "rows submitted", which double-counts across idempotent re-runs). |
| `api/fixtures/spp-backfill.json` (Task 8.5) | Committed fixture: `{generatedAt, source, contracts: [{name, id, eventCount}], events: RawEvent[]}` (dates as ISO strings, same convention as `__fixtures__/ct-events.json`), sorted by `compareArchiveOrder`. Real counts as of the 2026-08-01 run: `pool` 500, `pool_eurc` 0, `asp_membership` 0, `public_key_registry` 18 (518 total) — see Gotchas for why two contracts are genuinely empty. ~370KB, well under the brief's ~2MB gzip threshold. |

## Endpoints / Public surface

- `createDb(url: string): { db: Db; pool: Pool }` — `api/src/db/client.ts`.
- `createRepo(db: Db): IndexerRepo` — `api/src/db/repo.ts`.
- `IndexerRepo` / `RepoOps` — `api/src/db/repo.ts`. `RepoOps`: `insertEvents(rows)`, `getCursor(key)`, `setCursor(key, value)`, `insertCtActivity(rows)`, `insertBootnodePage(row)`, `getBootnodePage(cursorIn)`. `IndexerRepo` adds `withTransaction(fn)`.
- `loadEnv(source?): Env` — `api/src/lib/env.ts`. `Env = { DATABASE_URL: string; POLL_INTERVAL_MS: number; RPC_URL: string; BOOTNODE_URL: string; PORT: number }`.
- Tables + inferred row types (`events`/`EventRow`/`NewEventRow`, `ctActivity`/`CtActivityRow`/`NewCtActivityRow`, `cursors`/`CursorRow`/`NewCursorRow`, `bootnodePages`/`BootnodePageRow`/`NewBootnodePageRow`) — `api/src/db/schema.ts`.
- `fetchRpcEvents(rpcUrl, opts): Promise<EventsPage>`, `fetchBootnodeEvents(url, opts): Promise<EventsPage | {handoff: {fromLedger: number}}>`, `EventsPage`, `RawEvent`, `naturalEventId`, `eventIndexFromId`, `BootnodeCacheMissError`, `DEFAULT_FETCH_TIMEOUT_MS` — `api/src/lib/soroban-events.ts`, `api/src/lib/bootnode-client.ts`.
- `pollStream(source, repo, streamKey, ctTokenId?): Promise<void>`, `pollStreams(streams, repo, shouldStop?): Promise<StreamPollOutcome[]>`, `StreamSource`, `StreamConfig` (`{streamKey, source, ctTokenId?}`), `makeRpcSource(config)`, `makeBootnodeThenRpcSource(config)` — `api/src/modules/indexer/poller.ts`. Not re-exported from `api/src/index.ts` (only `worker.ts` consumes them so far).
- `normalizeCtEvent(raw: RawEvent, tokenId: string): CtActivityInsert[] | null`, `CtActivityInsert` (alias for `NewCtActivityRow`) — `api/src/modules/indexer/normalize-ct.ts`. Pure; not re-exported from `api/src/index.ts` (consumed directly by `poller.ts`).
- `main(): Promise<void>`, `tick(states, repo, now?, shouldStop?)`, `backoffDelayMs(n)`, `buildInitialStreamState(streamKey, source, ctTokenId?)` — `api/src/worker.ts`. Run via `pnpm --filter @grantfox/api worker` (dev, `tsx`) or `node dist/worker.js` (built).
- `encodeRpcModeLedgerCursor(ledger: number): string`, `isRpcModeCursor(cursor: string): boolean` (Task 8.5) — `api/src/modules/indexer/poller.ts`. Small additive exports of the module's own `rpc:`/`ledger:` cursor-prefix logic, so callers outside this file (the backfill loader) never duplicate the encoding.
- `decomposeToid(toid)`, `txToidForEvent(eventToid)`, `toidFromArchiveId(id)`, `mapArchiveRecordToRawEvent(record, txHash)`, `compareArchiveOrder(a, b)`, `ArchiveEventRecord` (Task 8.5) — `api/src/lib/spp-archive.ts`. Pure.
- `fetchArchiveEventsPage(baseUrl, contractId, opts)`, `fetchAllArchiveEvents(baseUrl, contractId, opts)`, `resolveTxHash(horizonUrl, eventToid, cache, fetchImpl?)`, `sleep(ms)` (Task 8.5) — `api/src/lib/spp-archive-client.ts`. I/O.
- `SPP_STREAM_KEY`, `initSppCursor(repo, streamKey, lastBackfilledLedger)`, `loadBackfillEvents(repo, rows, chunkSize?)` (Task 8.5) — `api/src/lib/spp-backfill-loader.ts`.
- None of the Task 8.5 surface is re-exported from `api/src/index.ts` — consumed only by the two backfill scripts and their tests, same "not a library import" rationale as `poller.ts`/`worker.ts`.
- `buildSppContractIds(): string[]`, `buildSppStreamKey(contractIds?): string` (Task 9) — `api/src/worker.ts`. The single source of truth for the SPP allow-list/stream-key, shared by `spp-backfill-loader.ts` and `modules/bootnode/handler.ts`.
- `RepoOps`/`IndexerRepo` (Task 9 additions) — `listEventsFromLedger(query: EventsWindowQuery): Promise<EventRow[]>`, `listEventsAfterId(query: EventsAfterIdQuery): Promise<EventRow[] | null>`, `getLedgerBounds(contractIds?: string[]): Promise<{min,max} | null>`, `listActivityForAccount(query: AccountActivityQuery): Promise<CtActivityRow[]>`, `listActivityForAccountBeforeId(query: AccountActivityBeforeIdQuery): Promise<CtActivityRow[] | null>` — `api/src/db/repo.ts`.
- `buildApp(deps: AppDeps): FastifyInstance` — `api/src/app.ts`. `main(): Promise<void>` — `api/src/server.ts` (run via `pnpm --filter @grantfox/api dev`/`start`).
- `registerActivityRoutes(app, repo: ActivityRepoDeps)`, `ActivityRepoDeps` — `api/src/modules/activity/routes.ts`. Mounts `GET /health`, `GET /contracts/:contractId/events`, `GET /accounts/:address/activity`.
- `registerBootnodeRoutes(app, deps: BootnodeHandlerDeps)` — `api/src/modules/bootnode/routes.ts`. Mounts `POST /rpc`.
- `handleGetEvents(rawParams, deps)`, `handleGetLatestLedger(deps)`, `BootnodeHandlerDeps`, `BootnodeRepoDeps`, `RpcOutcome<T>`, `JsonRpcError`, `WireEvent`, `GetEventsResult`, `GetLatestLedgerResult`, `RETENTION_HANDOFF_CODE (-32002)`, `CACHE_MISS_CODE (-32004)`, `INVALID_PARAMS_CODE (-32602)`, `INTERNAL_ERROR_CODE (-32603)` — `api/src/modules/bootnode/handler.ts`.
- `fetchUpstreamLatestLedger(rpcUrl, opts?): Promise<UpstreamLatestLedger>` — `api/src/lib/rpc-proxy.ts`.
- None of the Task 9 surface (`app.ts`/`server.ts`/`modules/activity`/`modules/bootnode`/`rpc-proxy.ts`) is re-exported from `api/src/index.ts` — `server.ts` is a standalone process entrypoint, same convention as `worker.ts`.

## Key methods

- `createDb` — `api/src/db/client.ts:11`.
- `createRepo` / `buildRepoOps` — `api/src/db/repo.ts:83`, `api/src/db/repo.ts:39`.
- `loadEnv` — `api/src/lib/env.ts:23`.
- `fetchRpcEvents` — `api/src/lib/soroban-events.ts:171`.
- `fetchBootnodeEvents` — `api/src/lib/bootnode-client.ts:150`.
- `pollStream` / `pollStreams` — `api/src/modules/indexer/poller.ts:103`, `api/src/modules/indexer/poller.ts:152`.
- `makeRpcSource` / `makeBootnodeThenRpcSource` — `api/src/modules/indexer/poller.ts:209`, `api/src/modules/indexer/poller.ts:248`.
- `normalizeCtEvent` — `api/src/modules/indexer/normalize-ct.ts:110`.
- `main` / `tick` / `backoffDelayMs` / `buildInitialStreamState` — `api/src/worker.ts:132`, `api/src/worker.ts:67`, `api/src/worker.ts:29`, `api/src/worker.ts:44`.
- `encodeRpcModeLedgerCursor` / `isRpcModeCursor` — `api/src/modules/indexer/poller.ts:228`, `api/src/modules/indexer/poller.ts:233`.
- `decomposeToid` / `txToidForEvent` / `toidFromArchiveId` / `mapArchiveRecordToRawEvent` / `compareArchiveOrder` — `api/src/lib/spp-archive.ts:76,85,90,110,130`.
- `fetchArchiveEventsPage` / `fetchAllArchiveEvents` / `resolveTxHash` — `api/src/lib/spp-archive-client.ts:88,107,139`.
- `initSppCursor` / `loadBackfillEvents` — `api/src/lib/spp-backfill-loader.ts:56,83`.
- `buildSppContractIds` / `buildSppStreamKey` — `api/src/worker.ts` (adjacent to `buildStreamStates`, near the top of the file).
- `buildApp` — `api/src/app.ts`. `main` (server) — `api/src/server.ts`.
- `registerActivityRoutes` — `api/src/modules/activity/routes.ts`.
- `registerBootnodeRoutes` — `api/src/modules/bootnode/routes.ts`.
- `handleGetEvents` / `handleGetLatestLedger` — `api/src/modules/bootnode/handler.ts`.
- `fetchUpstreamLatestLedger` — `api/src/lib/rpc-proxy.ts`.

## Dependencies

- `drizzle-orm` (`^0.45`, `drizzle-orm/node-postgres`) + `pg` (`^8`) — Postgres access.
- `drizzle-kit` (dev, `^0.31`) — migration generation (`db:generate`) and apply (`db:migrate`); config in `api/drizzle.config.ts`.
- `zod` (`^3.24`) — env validation.
- `@grantfox/shared` (`workspace:*`) — `TESTNET` config: `ct.token`/`ct.deployedAtLedger` (CT stream), `spp.pool`/`spp.poolEurc` (Task 9)/`spp.aspMembership`/`spp.publicKeyRegistry`/`spp.deploymentLedger`/`spp.nethermindBootnode` (SPP stream + bootnode allow-list, via `buildSppContractIds()`), `rpcUrl`/`spp.nethermindBootnode` as `env.ts`'s `RPC_URL`/`BOOTNODE_URL` defaults. Used by `env.ts` and `worker.ts` as of Task 7 (previously only declared). **Rebuild `packages/shared` (`pnpm --filter @grantfox/shared build`) after editing `config.ts`** — `api` imports it from `dist/`, not `src/`, so a source-only edit silently doesn't take effect until rebuilt (caught this during Task 9's own verification: `TESTNET.spp.poolEurc` read as `undefined` in `api`'s tests until the shared package was rebuilt).
- `@stellar/stellar-sdk` (pinned `16.2.0`) — `rpc`/`xdr` types, used by `soroban-events.ts`; `xdr`/`Address`/`scValToNative`, used by `normalize-ct.ts` (Task 8) to decode topics/value XDR into native addresses/amounts and raw ciphertext bytes. NOT used by the Task 9 `getLatestLedger` proxy (`rpc-proxy.ts`) — deliberately a raw `fetch`, see that file's Gotchas entry.
- `fastify` (`^5`) — declared per the task-5 brief for the API server; now used (Task 9): `app.ts`/`server.ts`/`modules/activity`/`modules/bootnode`.
- `@fastify/cors` (`^11.3.0`, Task 11) — permissive CORS (`{ origin: true }`) so the browser wallet can call this API cross-origin; see "Task 11 — CORS" above.
- `tsx` (dev, `^4.19.0`) — runs `worker.ts`/`server.ts` directly in dev (`pnpm --filter @grantfox/api worker`/`dev`) without a build step; also runs the two Task 8.5 scripts (`backfill:spp`, `backfill:spp:load`).
- (Task 8.5) `api/src/lib/spp-archive-client.ts` calls two public third-party APIs directly via global `fetch` (no new npm dependency): `https://api.stellar.expert/explorer/testnet` (the archive) and `https://horizon-testnet.stellar.org` (txHash resolution). Only reached by running `backfill-spp.ts`; never called by `worker.ts` or any test.
- Local Postgres: repo-root `docker-compose.yml` (`postgres:16-alpine`, user/pass/db `grantfox`, host port `${DB_HOST_PORT:-5433}`). `DATABASE_URL` default: `postgres://grantfox:grantfox@localhost:5433/grantfox` — set in repo-root `.env` and `.env.example`.
- `events.id` format (`${ledger}-${txHash}-${opIndex}-${eventIndex}`) matches `@ctd/sdk`'s `naturalEventId` (`packages/ctd-sdk/src/chain/events.ts:236`). `api/src/lib/soroban-events.ts` exports its own `naturalEventId`/`eventIndexFromId` that REPLICATE (not import) that logic — `@ctd/sdk` pulls in the zk-proving stack (`@aztec/bb.js`, `@noir-lang/noir_js`), which `@grantfox/api` has no other reason to depend on. `poller.ts` constructs `events` rows straight from `RawEvent` (which already carries the correctly-formatted `id`), not a third reimplementation.

### Task 12 — `POST /rpc` has a real, load-bearing consumer now

The browser wallet's shielded (SPP) rail points the `stellar-private-payments`
SDK's `bootnodeUrl` straight at this server (`app/src/lib/spp.ts`'s
`SPP_BOOTNODE_URL = ${VITE_API_URL}/rpc`). This is not a nice-to-have: testnet
Soroban RPC's retention window does not reach the pool's deployment ledger, so
the SDK's `Indexer::init` fails with `RpcSyncGap` and it *must* fall back to a
bootnode to build local pool state at all (`sdk/client/src/sync.rs:455-474`),
and Nethermind's own public bootnode rejects every filter set (see above).

Verified end to end on 2026-08-01 against a fresh browser profile: the SDK
logged `main RPC sync gap, trying bootnode at http://localhost:3801/rpc`, drew
its history from `POST /rpc` (4 requests, all `200`), then logged `bootnode
handoff, resuming on main RPC` — i.e. the `-32002` this server returns at its
tip lands in the SDK's handoff branch exactly as `handler.ts` intends, and the
client resumes on its own RPC. **Consequence: the SPP backfill
(`pnpm --filter @grantfox/api backfill:spp:load`) is a hard prerequisite for the
wallet's Shielded tab, not just for this API's own completeness.**

## Consuming CT events from this API (read before wiring up a client)

**`@ctd/sdk`'s `IndexerClient` (`packages/ctd-sdk/src/chain/indexer.ts`) and
anything built on top of it — `hybridFetchEvents`
(`packages/ctd-sdk/src/chain/event-source.ts:82`) and, transitively,
`StateEngine.sync()` (`packages/ctd-sdk/src/state/engine.ts:61,128`) —
MUST NOT be pointed at this API's `/contracts/:contractId/events` for CT
events as-is.** It will not throw or error visibly; it will silently
produce an EMPTY event stream, and if that empty stream reaches
`StateEngine`, the wallet's reconstructed balance state is wrong with no
error surfaced.

**Why**: `IndexerClient.parseIndexerEvent`
(`indexer.ts:186-202`) decodes `topic`/`value` assuming Goldsky's
JSON-ScVal wire shape (the format the reference `@ctd/indexer`'s Cloudflare
Worker + Goldsky pipeline actually produces). It calls `scvString(topics[0])`
(`indexer.ts:269-280`), which — when the value is ALREADY a plain string —
returns it verbatim (`typeof value === "string"` → return as-is). Our
`events` table stores `topic`/`value` as base64-encoded XDR strings (the
SAME representation `soroban-events.ts`/`bootnode-client.ts` always
produce; this API has no Goldsky pipeline and never will for this data),
so `topics[0]` is a base64 blob, not a decoded symbol like `"register"`.
`KNOWN.has(<base64 blob>)` (`indexer.ts:190`, `KNOWN` defined
`events.ts:119`) is false for every row, so `parseIndexerEvent` returns
`null` for every single event — no thrown error, just nothing decoded.

This propagates silently up the stack: `hybridFetchEvents`
(`event-source.ts:82-142`) merges the indexer leg's (empty) results with
the RPC leg's and returns normally — its own module doc
(`event-source.ts:17-26`) explicitly calls out that a THROWN indexer error
is deliberately NOT swallowed, specifically to avoid "turning one transient
failure into unrecoverable data loss" — but an indexer that returns
zero events with no error is indistinguishable from "there were genuinely
no CT events in that range," so this specific failure mode (wrong data
FORMAT, not a network/availability failure) is NOT caught by that
protection. `StateEngine.sync()` (`state/engine.ts:128`) would then
reconstruct balances from an incomplete event stream — the exact
"unrecoverable data loss" scenario the module doc warns about, just
reached by a different door than the one it defends against.

**Routing decision (controller-approved, made outside this task): the fix
lands in Task 11 (wallet), not here.** The correct shape is a THIN
XDR-decoding client that runs this API's base64-XDR rows through
`ctd-sdk`'s EXISTING XDR decoder — reusing `buildConfidentialEvent`
(`events.ts:154-205`, already exported and explicitly designed as the
source-agnostic "single source of truth for each event type's shape," per
its own doc comment: "Both `parseEvent` (RPC/XDR) and `parseIndexerEvent`
(Goldsky/JSON) call this with their own `addr`/`data` adapters, so the
field mapping cannot drift between sources") — NOT a server-side transform
that reshapes our stored rows into Goldsky-JSON on the way out. `parseEvent`
itself (`events.ts:378-392`) is the RPC/XDR reference implementation to
mirror (not import — it's a private, unexported function operating on the
SDK's already-parsed `rpc.Api.EventResponse`, not a raw string): decode
each base64 `topic`/`value` string back into an `xdr.ScVal` via the SDK's
standard `xdr.ScVal.fromXDR(str, "base64")` (the exact inverse of
`soroban-events.ts`'s own `.toXDR("base64")` encoding — same codec, round
trip), then supply `buildConfidentialEvent` with `addr`/`data` adapters
shaped like `parseEvent`'s own (`Address.fromScVal`, and a `dataMap`
equivalent to `events.ts:395-409`'s XDR-backed one). Building this belongs
to whichever task actually wires a wallet client to this API (Task 11 per
the current plan) — not duplicated here as a server-side Goldsky
transform, which the routing decision explicitly rejected (this API's
`events` table has no reason to ever hold Goldsky-shaped data; XDR is its
native, byte-accurate representation, and the SPP bootnode leg needs XDR
strings verbatim regardless — see the handler's module doc).

**This does NOT affect the bootnode-protocol leg** (`POST /rpc`'s
`getEvents`) — the SPP Rust SDK's OWN deserializer
(`sdk/stellar/src/rpc.rs`'s `Event` struct) expects base64-XDR `topic`/
`value` NATIVELY, byte-for-byte matching what this API already serves (see
the "deserializer byte-for-byte" citations in the task-9 report). Only the
REST `IndexerClient` path is affected.

## Gotchas & invariants

- `insertEvents` is on-conflict-do-nothing **by `id` only** — a re-inserted row with the same id but different payload is silently ignored (the first write wins). This is intentional for idempotent worker restarts, but means a genuine data-correction requires a manual `UPDATE`, not a re-insert.
- `insertBootnodePage` is also on-conflict-do-nothing by `cursorIn`, same idempotency rationale — a page fetch for an already-cached `cursorIn` is a no-op even if `response` would differ.
- `setCursor` is a real upsert (`onConflictDoUpdate`), unlike the two above — cursors are meant to move forward and always reflect the latest value.
- `withTransaction`'s callback receives a `RepoOps` bound to the transaction handle (not the outer `IndexerRepo`), so it can call any insert/query helper but not nest another `withTransaction`.
- `ct_activity.amount` is the only nullable column in the schema; everything else (including `bootnode_pages.cursor_out` / `last_event_ledger`) is `NOT NULL` per the task-5 brief's literal column list — there was no signal to make anything else optional.
- `cursors.updated_at` and `bootnode_pages.created_at` default to `now()` at the DB level (`defaultNow()`), not application code — this wasn't spelled out in the brief but matches the "troqpay shape" convention for timestamped audit tables.
- `api/tsconfig.json` excludes `src/**/*.test.ts` from the `build` output — test files are type-checked by `vitest` (esbuild transpile, not full `tsc`) and separately by a full-tree `tsc --noEmit` during review, not by `pnpm build`.
- `fetchRpcEvents`/`fetchBootnodeEvents` both derive `RawEvent.id` as `${ledger}-${txHash}-${opIndex}-${eventIndex}`; `opIndex`/`txIndex` come directly off sdk-16's `operationIndex`/`transactionIndex` fields (no toid bit-masking needed, unlike `@ctd/sdk`'s older-SDK-targeting `rpcEventCoords`), but `eventIndex` still has no dedicated field on either source and is parsed from the second segment of the event `id` (`<toid>-<eventOrder>`) by the shared `eventIndexFromId` helper.
- `RawEvent.topic`/`valueXdr` are base64-encoded XDR strings for BOTH sources, not decoded native values — `fetchRpcEvents` re-encodes the SDK's parsed `xdr.ScVal`s via `.toXDR("base64")`; `fetchBootnodeEvents` passes the bootnode's already-base64 wire strings straight through. Decoding to CT-specific native values (event names, addresses, amounts) is left to the normalizer (Task 8), not done here.
- `fetchRpcEvents` throws if an `EventResponse.contractId` is `undefined` — it does NOT fall back to a single "requested" contract id (unlike troqpay's `soroban-gateway.ts`, whose `getContractEvents` fell back to its one request-scoped `contractId`), because this adapter filters by potentially MULTIPLE `contractIds` and a wrong fallback would silently mislabel a row's contract.
- `fetchBootnodeEvents` throws if a mapped event is missing `operationIndex`/`transactionIndex`/`txHash`/`inSuccessfulContractCall` (all `Option` on the bootnode's Rust `Event` struct) — the `events` table requires them `NOT NULL` and the id can't be built without `txHash`/`operationIndex`, so this adapter fails fast rather than guessing.
- The bootnode's wire params nest `cursor`/`limit` under `params.pagination`, NOT flat on `params` — `GetEventsParams` is `#[serde(deny_unknown_fields)]` server-side, so a flat body is rejected. Neither `soroban-events.ts` nor `bootnode-client.ts` retries or logs; `poller.ts`/`worker.ts` own all retry/backoff policy (including on `BootnodeCacheMissError` — an ordinary caught error, backed off like any other stream failure, no special-casing needed).
- **Both network adapters are timeout-bounded, `DEFAULT_FETCH_TIMEOUT_MS` = 30s, injectable per-call as `timeoutMs`** (Task 7 review fix — before this, a hung fetch on one stream blocked `pollStreams`' sequential loop indefinitely, and since `worker.ts`'s `while` loop only checks its shutdown flag BETWEEN `tick()` calls, a hang meant SIGTERM never produced the documented clean exit). `fetchBootnodeEvents` uses a plain `AbortController`+`setTimeout` (its own `fetch` call). `fetchRpcEvents` is the more interesting one: `RpcServer.Options.timeout` — the CONSTRUCTOR option `server.d.ts` documents — is a **dead type in the installed sdk-16.2.0 build**; `new RpcServer(url, opts)`'s actual (transpiled) constructor only ever reads `opts.headers`/`opts.allowHttp`, never `opts.timeout` (verified by reading `rpc/server.js`, not trusting the `.d.ts`). The mechanism that DOES work: `server.httpClient.defaults.timeout = ms`, mutating the mutable `HttpClientDefaults` object the SDK's own doc-comment example shows is a supported pattern (`server.httpClient.defaults.headers[...] = ...`) — `getEvents`'s call path merges `httpClient.defaults` into every request and (since no `maxRedirects`/`maxContentLength` is set) routes through the `feaxios` dependency's `instance.request`, which genuinely honors `config.timeout` via `AbortSignal.timeout` and rejects with a plain `AxiosError` on expiry. Covered by a "wiring" test in `soroban-events.test.ts` (captures `this.httpClient.defaults.timeout` inside a `getEvents` mock) rather than a live-hang test, since the SDK's `getEvents` is normally mocked wholesale in tests (`vi.spyOn(rpc.Server.prototype, "getEvents")`), bypassing the HTTP layer entirely.
- `pollStreams(streams, repo, shouldStop?)`'s `shouldStop` is checked only BETWEEN streams, never mid-flight — a stream already in progress when a shutdown signal arrives always runs to completion or its own timeout. This bounds `tick()`'s shutdown-triggered worst case to "at most one stream's timeout" rather than "every remaining stream's timeout summed," without restructuring the sequential for-loop into concurrent execution. `worker.ts`'s `main()` passes `() => shuttingDown` as `tick`'s `shouldStop`.
- **Cursor-value encoding is meaningful, not opaque, for the two `poller.ts` source factories** — see the file's module doc comment for the full state machine. Short version: `null` = start from configured `startLedger`; a bare string = an opaque cursor from the underlying source passed straight back; `ledger:<N>` = a synthesized fallback when a page reports `cursor: null` (EventsPage's "no further cursor" case — neither `fetchRpcEvents` nor `fetchBootnodeEvents` accepts a bare ledger number as `cursor`, so this marker is decoded back into a `startLedger` fetch, never sent to the network as-is); `handoff:<N>` (SPP only) = the literal transitional marker the task brief asks for, persisted the instant the bootnode reports a retention handoff; `rpc:<...>` (SPP only) = wraps EVERY subsequent cursor once handed off, which is what makes the handoff survive a worker restart — `makeBootnodeThenRpcSource` decides "bootnode or RPC?" purely by reading this prefix off the persisted string, never from in-memory state.
- `pollStream` reads the cursor and calls `source.fetchPage` (network I/O) BEFORE opening the transaction — a transaction is never held open across a network call. Only the insert + `setCursor` happen inside `repo.withTransaction`.
- A page with `nextCursor !== null` always advances the cursor even when `events` is empty (invariant 2) — `insertEvents([])` is `RepoOps`'s documented no-op, `setCursor` still runs. `pollStream` only skips `setCursor` if a `StreamSource` ever returns a literal `null` `nextCursor`, which neither shipped factory does (they always synthesize `ledger:`/`handoff:`/`rpc:` instead) — this is a defensive branch, not exercised by the shipped sources.
- **`api/package.json`'s `test` script runs `vitest run --no-file-parallelism`** (Task 7) — vitest's default is to run test FILES concurrently, and with two DB-integration test files now sharing the same live local-Postgres tables (`repo.test.ts` and `poller.test.ts`'s `describe.skipIf(!DATABASE_URL)` block, both `beforeEach`-truncating `events`/`ct_activity`/`cursors`/`bootnode_pages`), concurrent files raced: one file's truncate could fire mid-assertion in the other, observed as a rare, non-deterministic `ct_activity` row-count failure in `repo.test.ts` (not caused by a bug in either file's own logic — confirmed reproducible only under file-level parallelism, gone after 10/10 clean runs with the flag). Any FUTURE integration test file added to this package should assume it may run interleaved with these two unless this flag stays in place.
- **`normalize-ct.ts`'s on-chain field names follow the FIXTURES, not the checked-out `resources/stellar-contracts` Rust source (Task 8, verified 2026-08-01).** The deployed testnet CT contract's real event data-map field names (`r_e`, `sigma`, `b_tilde`, `b_aud_s`, `v_tilde`, `v_aud_r`, `r_aud_r`, `v_aud_s` — decoded straight from `__fixtures__/ct-events.json`'s `valueXdr`) match the vendored `@ctd/sdk`'s `packages/ctd-sdk/src/chain/events.ts` `buildConfidentialEvent` mapping EXACTLY (event names, topic layout, and both the snake_case on-chain names and their camelCase row-field names `rE`/`vTilde`/`sigma`/`bTilde`/`vAudR`/`rAudR`/`vAudS`/`bAudS` — cross-checked line-by-line against `events.ts:167-193`). They do NOT match the branch checked out at `resources/stellar-contracts` (`feat/confidential-verifier-ultrahonk`, `packages/tokens/src/confidential/mod.rs`), which has since renamed several fields (e.g. `b_aud_s` → `b_tilde_aud_s`) — the deployed contract predates that rename. If `resources/stellar-contracts` is ever updated to a build matching the rename, `normalize-ct.ts` and its fixtures need re-verifying against a fresh live capture before trusting the Rust source over the fixtures again.
- `normalize-ct.ts`'s `ciphertexts` values are `0x`-prefixed lowercase hex of the RAW on-chain `BytesN<32>`/`BytesN<64>` bytes (`val.bytes()`, no curve-point decode) — a deliberate choice to avoid depending on `@ctd/sdk`'s crypto helpers (`pointFromBytes`/`fromBytesBE`, which pull in the zk-proving stack `@aztec/bb.js`/`@noir-lang/noir_js`, same rationale as the `events.id` gotcha above). The wallet client (Task 11) decodes these hex strings itself with the same byte layout those helpers expect — this is why `withdraw`'s `rE` (a `Point`, 64 bytes) and `sigma`/`bTilde`/`bAudS` (field elements, 32 bytes) can share one `hexBytes()` helper: the normalizer never needs to know which is which, only the wallet does.
- **`withdraw` produces a row for `from` ONLY, not `to`** (task-8-brief.md's literal wording) — `to` merely receives plain SEP-41 tokens leaving the confidential system and isn't a CT-tracked account for this event. `deposit` and `transfer` both collapse to a SINGLE row (not two identical ones) when `from === to` — a self-deposit/self-transfer is a real on-chain pattern in the fixtures (e.g. fixture 8, ledger 3900642) and emitting two identical rows would double-count that one action in that one account's activity feed.
- `normalizeCtEvent` returns `null` (not an empty array) for anything it doesn't map: wrong `contractId`, zero topics, a non-symbol `topic[0]`, or a real CT-emitted event outside its five-name whitelist (`spender_transfer`/`set_spender`/`revoke_spender`/the compliance events/the four constructor-setter events seen live in the fixtures).
- **Per-event error isolation is `poller.ts`'s job, not `normalize-ct.ts`'s (review fix, 2026-08-01).** `normalizeCtEvent` is deliberately fail-fast: a malformed/missing value field (`requireField`) or an undecodable topic throws, it does not return `null`/a partial row/swallow the error. Originally `pollStream`'s wiring called it bare inside `events.flatMap(...)` — so ONE malformed event threw out of the whole `repo.withTransaction` callback, rolling back the ENTIRE page's raw-event insert and skipping the cursor advance; the next poll would refetch the identical poisoned page and fail identically, forever (a permanent stall, not just a dropped event — worse than doing nothing). Fixed: `pollStream` now calls `normalizeCtEvent` inside a per-event try/catch, `console.error`-logs the failure (with the offending event's `id`, so it's greppable/alertable in production logs) and continues — that ONE event contributes zero `ct_activity` rows but its raw `events` row still inserts, every other event on the page still normalizes normally, and the cursor still advances past the whole page. The foreseeable trigger this guards against is the one `normalize-ct.ts`'s own module doc already flags: a redeployed CT contract whose event field names have drifted (the `b_aud_s` → `b_tilde_aud_s` rename already observed on `resources/stellar-contracts`' current branch, not yet on the deployed contract this normalizer targets). Covered by `poller.test.ts`'s "poisoned CT event" case (asserts both raw events insert, only the good event's activity row inserts, the cursor still advances, and the log fires with the event id) and `normalize-ct.test.ts`'s fail-fast case (asserts the throw itself, proving `normalizeCtEvent` was never made defensive — only its caller was).
- `pollStream`'s per-event loop treats a `null` return the same as an empty array (`if (rows !== null) activityRows.push(...rows)`), so the `null`-vs-`[]` distinction above only matters to `normalizeCtEvent`'s own callers/tests, not to the wiring.
- **Live-verification blocker (Task 7, 2026-07-31): Nethermind's public bootnode (`https://bootnode.dev-nethermind.xyz`) rejects every `getEvents` request with `-32602 unsupported filters`** — verified via direct `curl`, not just through our client: the canonical SPP testnet `pool`/`publicKeyRegistry` ids (matching BOTH `packages/shared/src/config.ts` AND the vendored reference repo's own `resources/stellar-private-payments/deployments/testnet/deployments.json`), a garbage contract id, and empty `filters` all get the identical error. `tools/bootnode/src/rpc.rs:78`'s `is_allowed_filters` check rejects any request whose `contractIds` don't exactly match the bootnode's own configured allow-list — so this is an operational/deployment mismatch on the PUBLIC bootnode's side (or it's scoped to a different contract set than this repo's vendored copy), not a wire-shape bug in `bootnode-client.ts` (confirmed byte-for-byte against `messages.rs`'s own `for_contracts` test helper). `getLatestLedger` against the same bootnode succeeds, so the service itself is up. Compounding this: the SPP pool's `deploymentLedger` (3773948) is already outside the live RPC's retention window (currently ledger ~3780722+ only) — so right now there is NO live path (bootnode blocked, RPC out of retention) to backfill SPP history from deployment. The RPC-after-handoff leg itself was verified structurally live (see Testing) — it is the bootnode's contract allow-list that blocks the SPP stream end-to-end today. Re-check by re-running the two `curl` probes in the task-7 report if picking this back up. **Resolved for history recovery by Task 8.5** (below) — the RPC/bootnode dead-ends themselves are unchanged and would still block a truly fresh backfill without stellar.expert's archive.
- **Task 8.5's Horizon txHash resolution refines the brief's literal `cursor=<toid-1>` recipe.** Verified live: Horizon's `/transactions` collection only has paging tokens at `opOrder=0` (one entry per TRANSACTION, not per operation) — for an event whose TOID has `opIndex > 0` (the common case: most operations emit several events), `cursor = eventToid - 1` overshoots into the tx AFTER the correct one. The fix (`spp-archive.ts`'s `txToidForEvent`, used by `spp-archive-client.ts`'s `resolveTxHash`): zero the low 12 (opOrder) bits of the event's TOID FIRST, then subtract 1. `resolveTxHash` additionally asserts the returned transaction's own `paging_token` equals the expected tx-level TOID exactly (the brief's own "paging_token's TOID prefix matches" check) rather than trusting Horizon's cursor semantics blindly — this assertion is what would have caught the naive recipe's bug had it not been caught by manual verification first. Full transcript (real toid, real wrong-vs-right tx hash) in `spp-archive.ts`'s module doc.
- **RESOLVED by Task 9 — `SPP_STREAM_KEY` (`spp-backfill-loader.ts`) is no longer a hand-replication of `worker.ts`'s SPP stream key; it imports `buildSppStreamKey()`.** (Kept for history: `worker.ts`'s `buildStreamStates()` was previously private/unexported, so Task 8.5 hand-copied the computation and flagged the drift risk explicitly — "the safer long-term fix would be exporting a `buildSppStreamKey()` helper from `worker.ts`." Task 9 did exactly that.) The literal-string test in `spp-backfill-loader.test.ts` still pins the value (now `spp:<pool>,<poolEurc>,<aspMembership>,<publicKeyRegistry>`, 4 contracts) as belt-and-suspenders.
- **RESOLVED by Task 9 — `packages/shared/src/config.ts` now has `TESTNET.spp.poolEurc`.** (Kept for history: Task 8.5 found `config.ts`'s `TESTNET.spp` block covered only 3 of the SDK's 4-contract sync set — `pool`/`aspMembership`/`publicKeyRegistry`, missing the EURC pool `CAJJT5YV4BMFTHEOO5FGO2G56TEJKM4G4FW7FS4DYBLLLLHSAYUZWT74` — and flagged it rather than fixing it as an out-of-scope side effect.) `backfill-spp.ts`'s own `POOL_EURC_CONTRACT_ID` hardcode was DELIBERATELY left as-is per the task-9 brief ("Task 8.5's script may keep its hardcode") — it's a one-shot archival-recovery script, not a live code path, so there's no drift risk worth the touch; its value is identical to `TESTNET.spp.poolEurc`.
- **RESOLVED by Task 9 — the worker's live SPP stream now tracks all 4 SPP contracts, not 2.** (Kept for history: Task 7's `worker.ts` originally set `sppContractIds = [TESTNET.spp.pool, TESTNET.spp.publicKeyRegistry]`, so `asp_membership`/the EURC pool's backfilled history sat in `events` with no live stream polling for NEW events on them.) `buildSppContractIds()` now returns all 4 (`pool`, `poolEurc`, `aspMembership`, `publicKeyRegistry`) and `worker.ts`'s `buildStreamStates()` uses it directly — this was closed as a carried-gap prerequisite for Task 9's bootnode endpoint (its allow-list must be set-equal to the SDK's `all_contract_ids()` request, which is these same 4 contracts).
- **The bootnode handler's handoff/cache-miss semantics (Task 9) DELIBERATELY diverge from the reference `tools/bootnode/src/rpc.rs`'s.** The reference is a partial, asynchronously-populated CACHE in front of the real RPC (`ledger_tip`/`in_sync` atomics, a background indexer that may not have reached a given page yet, `is_terminal_empty` tip-self-loop detection) — hence its multi-branch `cache_miss_or_handoff`. OUR bootnode serves synchronously from a FULLY MATERIALIZED `events` table (Task 8.5's full backfill + continuous live-follow), so there's no "known but not yet cached" state to track. Collapsed to three rules (full rationale in `modules/bootnode/handler.ts`'s module doc): cache miss (`-32004`) = EITHER the `pagination.cursor` doesn't resolve to any row we have for the allowed contract set, OR (review fix — see below) we have indexed NOTHING at all yet for the allowed set (tip unknown, mirroring the reference's `!tip_known -> cache_miss_for_tip(0)`, `rpc.rs:171-174`); handoff (`-32002`, `fromLedger = ourTip + 1`) = ONLY once a tip is known and the resolved query (by `startLedger` or `cursor`) returns zero rows — equivalent to "beyond our indexed tip" for the `startLedger` case. Verified live against the real server + real backfilled data for all 5 of the brief's required cases (valid page, both-params error, unsupported-filter error, handoff, cache miss) — transcript in the task-9 report.
- **Review fix: an empty/not-yet-backfilled `events` table used to return a FALSE `-32002` handoff (`fromLedger: 0`) instead of `-32004`.** The bug: `getLedgerBounds` returning `null` (zero rows for the allowed contract set — nothing backfilled yet) was treated as "tip = -1, handoff at fromLedger 0" — a real handoff response, telling the client to abandon this bootnode and resume on its OWN RPC. Consequence, traced through the SPP SDK's actual sync loop (`sdk/client/src/sync.rs`, verified by reading, not assumed): `-32002` is matched by `is_retention_handoff`/`is_retention_handoff_err` (`sync.rs:311,344`, checking `RETENTION_HANDOFF_CODE = -32_002` at `sync.rs:22`) and triggers IMMEDIATE abandonment — `storage.clear_indexing_cursors()` then `"resuming on main RPC"` (`sync.rs:404-407,426-432`), no retry — landing on a main RPC that's retention-gapped for SPP history (see the "Live-verification blocker" gotcha below) and failing with a confusing error. A genuine `-32004` does NOT match `is_retention_handoff` and instead falls into the generic-error retry branch (`sync.rs:434-446`), which retries with a backoff sleep up to `BOOTNODE_CATCH_UP_MAX_FAILURES = 10` rounds (`sync.rs:19`) before giving up — giving a slow/not-yet-run backfill real time to land instead of bailing on the first response. Fixed: `bounds === null` now returns `CACHE_MISS_CODE` with the reference's own "bootnode warming up; retry later" message (`rpc.rs:267`), matching `cache_miss_for_tip(0)` exactly. Covered by `handler.test.ts`'s "cache miss (NOT -32002 handoff) when we have indexed nothing at all" case.
- **`getEvents`'s response `latestLedger`/`latestLedgerCloseTime` are the REAL upstream network tip (a live proxy call, same mechanism as `getLatestLedger`), NOT our own indexed-events max** — this matters functionally, not just cosmetically: the SPP Rust SDK's own indexer compares `progress_ledger >= latest_ledger` to decide "have I caught up this round" (`resources/stellar-private-payments/sdk/stellar/src/indexer.rs:155`), so self-reporting our indexed max here would make every client think it reached the chain tip the moment it reached whatever WE happen to have indexed. `oldestLedger` IS our own archive floor (`getLedgerBounds`'s `min`, a real value — free, same query call as the tip-for-handoff check); `oldestLedgerCloseTime` is the oldest row IN THE RETURNED PAGE (not a 3rd DB round-trip to fetch the archive floor row's own timestamp) — deliberate, since grep confirms neither `oldestLedger`/`oldestLedgerCloseTime` is consumed by the SDK's sync logic (only `latestLedger`/`cursor` are).
- **Events are ordered by the REAL chain-position tuple `(ledger, txIndex, opIndex, eventIndex)`, not string `id` order** — `listEventsFromLedger`/`listEventsAfterId` (`db/repo.ts`, Task 9) and the REST/bootnode routes that page through them. `events.id` (`${ledger}-${txHash}-${opIndex}-${eventIndex}`) has no zero-padding, unlike the reference `@ctd/indexer`'s fixed-width Goldsky ids that `ORDER BY id ASC` can rely on — a naive port of that pattern here would sort e.g. ledger `1000000000` before `999999999` lexicographically. `repo.test.ts` proves this with a deliberately mixed-digit-width fixture.
- **`api/src/modules/activity/routes.ts`'s `/contracts/:contractId/events` guarantees wire-envelope compatibility with `@ctd/sdk`'s `IndexerClient`, but NOT CT-event content decoding through it — `IndexerClient`/`hybridFetchEvents` MUST NOT be pointed at this API for CT events as-is.** See the dedicated "Consuming CT events from this API" section above for the full explanation (base64-XDR vs. Goldsky-JSON, why it fails SILENTLY not loudly, and the required Task 11 adapter approach) — this entry is just a pointer so it's not missed while skimming Gotchas.
- **Two of the four SPP contracts genuinely have ZERO on-chain events as of 2026-08-01** — verified both via `order=desc&limit=1` (empty `_embedded.records`) and a full `order=asc` page fetch, not just an artifact of pagination: `pool_eurc` and `asp_membership`. Only `pool` (500) and `public_key_registry` (18) have activity. Not a bug in the fetch logic — `pool` matches the brief's expected 500 exactly, and a live re-run of `backfill-spp.ts` reproduced identical results (byte-for-byte, excluding `generatedAt`) on a second independent run.
- **`api/scripts/*.ts` are NOT part of `api/tsconfig.json`'s `include` (`src/**/*` only)** — they run via `tsx` directly (same convention as `worker.ts`'s dev script and the repo-root `scripts/smoke-ct.ts`) and are excluded from `pnpm --filter @grantfox/api build`. Manually typechecked clean during Task 8.5 review via a throwaway `tsconfig` extending the real one with `scripts/**/*` added to `include` — not committed, since it would otherwise change `build`'s output scope.
- `fetchJsonWithRetry` (internal to `spp-archive-client.ts`) retries BOTH non-OK 429/5xx responses AND thrown network-level errors (DNS failure, connection reset — `fetch` rejects rather than resolving for these, so they never reach a `res.ok` check). The network-error branch was added mid-task after a live `backfill-spp.ts` run hit a genuine `ECONNRESET` partway through — without it, one transient blip anywhere in a run touching hundreds of events would abort the whole script with zero partial progress saved (the fixture is only written at the very end, after every event resolves). Both branches share `backoffDelayMs` (imported from `worker.ts`, not re-implemented), capped at `MAX_RETRIES = 6`.
- **`TESTNET.ct.deployedAtLedger` (`packages/shared/src/config.ts:31`, `3900251`) will age out of public Soroban RPC retention around 2026-08-07** — the same class of problem the SPP stream already hit (see the "Live-verification blocker" gotcha above: RPC retention is a rolling ~120k-ledger window). This does NOT threaten the deployed `production` environment: this API's own `events` table already holds the CT stream's full archival history from `deployedAtLedger` forward (continuously appended by `worker.ts`'s live poll, same durable-source pattern the SPP rail depends on — see "Task 12 — `POST /rpc` has a real, load-bearing consumer now" above), and the wallet app's CT sync path already falls back to this indexer via `ct-indexer.ts`'s `ApiIndexerClient` for anything outside RPC's own window (`hybridFetchEvents`, `event-source.ts:82`). What it DOES threaten: standing up a genuinely FRESH environment (new DB, no prior backfill) after that date, straight from live RPC alone — `fetchRpcEvents` starting at `deployedAtLedger` would simply return nothing for the gap, identical in kind to the SPP pool's `deploymentLedger` (3773948) already being outside RPC retention today. There is currently no CT-specific equivalent of `backfill-spp.ts`/`spp-archive.ts` (stellar.expert-archive recovery) — if a from-scratch CT backfill is ever needed past this date, that's the mechanism to port.

## Testing

- `pnpm --filter @grantfox/api test` (`vitest run --no-file-parallelism`) — offline: 98 tests (`env.test.ts` ×7, `schema.test.ts` ×5, `soroban-events.test.ts` ×9, `bootnode-client.test.ts` ×10, `poller.test.ts` ×13 of 16, `worker.test.ts` ×13, `normalize-ct.test.ts` ×18 [Task 8, incl. the review-fix self-transfer + fail-fast cases], `spp-archive.test.ts` ×13 [Task 8.5], `spp-backfill-loader.test.ts` ×10 [Task 8.5]) always run; `repo.test.ts` ×6 and `poller.test.ts`'s 3 `describe.skipIf(!DATABASE_URL)` integration cases (the original invariant-1 proof plus Task 8's two CT-wiring cases) auto-skip when `DATABASE_URL` is unset (9 tests skipped total, 107 tests overall).
- Integration: `docker compose up -d postgres` (repo root), then `DATABASE_URL=postgres://grantfox:grantfox@localhost:5433/grantfox pnpm --filter @grantfox/api test` — all 107 tests pass, including `repo.test.ts`'s transaction commit/rollback, `poller.test.ts`'s real-Postgres invariant-1 proof (a NOT NULL violation mid-`pollStream` rolls back both the insert and the cursor), the offline in-memory-`FakeRepo` invariant-1 proof (stage/commit/rollback semantics, not a stub), (Task 8) `poller.test.ts`'s real-Postgres CT-wiring proofs: a `register` event lands both its `events` row and its normalized `ct_activity` row in ONE transaction, and a mid-transaction failure (a bad second event) rolls back BOTH tables together, not just the cursor, and (review fix) the offline poisoned-event case: one malformed CT event on a page still lets both raw events insert, the good event's activity row insert, and the cursor advance, with the failure logged including the event id.
- `soroban-events.test.ts`/`bootnode-client.test.ts` mock `rpc.Server.prototype.getEvents`/global `fetch` respectively — neither hits a live network endpoint. `poller.test.ts`/`worker.test.ts`/`normalize-ct.test.ts` are fully offline (fake `StreamSource`s / injected `fetchEvents`/`fetchBootnode` / committed fixture JSON), except the real-Postgres cases above.
- Migration apply: `docker compose up -d postgres` then `DATABASE_URL=... pnpm --filter @grantfox/api exec drizzle-kit migrate` — verified applying `0000_unusual_captain_marvel.sql` cleanly to a fresh volume, `\dt`/`\d <table>` column-by-column matches the brief.
- Live (Task 7, testnet): `DATABASE_URL=... POLL_INTERVAL_MS=3000 pnpm --filter @grantfox/api worker` — CT stream landed 13 real rows in `events` (ledger 3900254–3900654, our smoke-test transactions) and its cursor round-tripped correctly on a second run (still 13 rows — no duplicates, live invariant-3 evidence). SPP stream hit the bootnode blocker above on every tick without affecting the CT stream (live invariant-5 evidence). The RPC-after-handoff leg was verified structurally by calling `pollStream`+`makeRpcSource` against the real DB for the SPP contract ids from a within-retention ledger (mechanism works: cursor persists; 0 events, because there is genuinely no on-chain activity for these contracts anywhere in the RPC's current ~120k-ledger retention window — confirmed by scanning it end to end). SIGINT/SIGTERM handling was verified against the BUILT artifact (`node dist/worker.js`, a single process — the shape a real deployment would run) via direct `kill`: both signals produce `[worker] received SIG..., shutting down after the current tick` → `[worker] shut down cleanly` and a clean process exit. (A `kill -TERM` sent to only the outer PID of a `pnpm`/`npx tsx` dev invocation does NOT reach the actual runtime process — those wrap 2–3 levels of child processes; a real terminal's Ctrl+C and a production single-process deploy both avoid this, but scripted single-PID signal tests against the dev command are misleading. Test against `node dist/worker.js` instead.) Full details, including the exact `curl` probes: task-7 report (`.superpowers/sdd/2026-07-31-privacy-wallet/task-7-report.md`, gitignored).
- Live (Task 8, testnet, 2026-08-01): `DATABASE_URL=... timeout 25s pnpm --filter @grantfox/api worker` against the same real CT contract — the 13 live events from the CT stream landed both their `events` rows AND normalized `ct_activity` rows (10 rows: 4 `register` + 1 self-`deposit` + 2 `merge` + 2 `transfer` + 1 `withdraw` — the 4 constructor-setter events correctly produced zero `ct_activity` rows). Spot-checked one `transfer` row's `ciphertexts` jsonb in Postgres directly: all 8 hex fields present and byte-identical to `normalize-ct.test.ts`'s hand-decoded expectations. SPP stream failed on the known dead-bootnode issue (unrelated to this task, tracked by Task 8.5) without affecting the CT stream — confirms per-stream isolation still holds with the new wiring. DB truncated back to empty after this run.
- `pnpm --filter @grantfox/api build` (`tsc -p tsconfig.json`) — verified clean.
- **Live (Task 8.5, testnet + stellar.expert + Horizon, 2026-08-01):**
  - `pnpm --filter @grantfox/api exec tsx scripts/backfill-spp.ts` — fetched all 4 SPP contracts' full archive history: `pool` 500 (matches the brief's expected count exactly), `pool_eurc` 0, `asp_membership` 0, `public_key_registry` 18 (518 total). The two zero-count contracts were double-checked with an independent `order=desc&limit=1` probe (empty) before trusting the `order=asc` pagination loop's own "no more pages" logic. Wrote `api/fixtures/spp-backfill.json` (382KB — well under the brief's ~2MB gzip threshold, committed uncompressed).
  - **Determinism**: re-ran the script a second time (a real second live fetch + resolve pass, not a replay) — the resulting fixture is byte-identical to the first EXCLUDING `generatedAt` (deep-equal check on the parsed JSON). The second run also live-caught a genuine `ECONNRESET` mid-run against stellar.expert, which the `fetchJsonWithRetry` network-error-retry hardening (see Gotchas) recovered from transparently.
  - `DATABASE_URL=postgres://grantfox:grantfox@localhost:5433/grantfox pnpm --filter @grantfox/api exec tsx scripts/load-backfill.ts` against a freshly-truncated local docker Postgres — inserted 518/518 rows, per-contract counts read back from the DB itself (not just "rows submitted") matched the fixture exactly (`pool` 500, `pool_eurc` 0, `asp_membership` 0, `public_key_registry` 18), and initialized the `spp:<pool>,<publicKeyRegistry>` cursor to `rpc:ledger:3898970` (`no-existing-cursor` reason). **Idempotency**: ran the loader a SECOND time against the same DB — `events` count stayed at 518 (on-conflict-do-nothing, no duplicates) and the cursor was reported `left untouched (already-rpc-mode)`, still `rpc:ledger:3898970` — proves a re-run can never regress real worker progress.
  - **Worker never touches the bootnode, verified by running it**: `DATABASE_URL=... BOOTNODE_URL=https://bootnode.invalid.nonexistent.example POLL_INTERVAL_MS=4000 timeout 16s pnpm --filter @grantfox/api exec tsx src/worker.ts` — deliberately pointed `BOOTNODE_URL` at a non-resolving host so ANY accidental bootnode call would surface immediately as a loud DNS/fetch error in the worker's per-stream failure logging. Across ~4 ticks (16s / 4s interval) the ONLY output was the single startup line (`[worker] starting — streams: ct:..., spp:...`) — zero errors, meaning the SPP stream's `fetchPage` took the `cursor.startsWith("rpc:")` branch every single tick and never called `fetchBootnode` at all (structurally guaranteed by `poller.ts`'s `makeBootnodeThenRpcSource`, already unit-proven by `poller.test.ts`'s invariant-4 case — this run proves it live, end to end, against the real worker binary). Confirmed by re-querying Postgres after the run: the SPP cursor advanced from `rpc:ledger:3898970` to a genuine RPC-issued opaque cursor token (`rpc:0016813310905155583-4294967295`), proving the (real, default `TESTNET.rpcUrl`) RPC call actually succeeded and returned a real page (0 new SPP events — expected, since the archive already covered "to tip at run time"); `pool`/`public_key_registry` counts stayed at exactly 500/18 (no duplicates from the live RPC re-observing already-backfilled ledgers, which it can't — its `startLedger` is past them).
  - **Rows join cleanly with live rows**: the SAME worker run's CT stream (unrelated to this task, untouched cursor `ct:<token>`, starting fresh) landed 13 real live-fetched rows into the SAME `events` table alongside the 518 backfilled SPP rows with zero constraint errors (total 531) — direct proof that backfilled and live-RPC rows share one schema/id-format without collision. (The SPP stream itself had no NEW live events to co-insert against the backfill, since the archive already covers current tip — the CT stream's live insert is the concrete "backfilled + live coexist" evidence for this run.)
  - `pnpm --filter @grantfox/api test` (`vitest run --no-file-parallelism`, `DATABASE_URL` set) — 107/107 pass (84 prior + 23 new: `spp-archive.test.ts` ×13, `spp-backfill-loader.test.ts` ×10), 0 skipped, 0 failed.
  - `pnpm build` (root, all 3 buildable workspace packages) — clean.
  - Full transcript, exact commands, and raw counts: task-8.5 report (`.superpowers/sdd/2026-07-31-privacy-wallet/task-8.5-report.md`, gitignored).
- **Task 9 (2026-08-01)**: `pnpm --filter @grantfox/api test` (`DATABASE_URL` set) — 173/173 pass, 0 skipped, 0 failed (107 prior + 66 new: `worker.test.ts` +2, `spp-backfill-loader.test.ts`'s key updated, `repo.test.ts` +12, `env.test.ts` +2, `rpc-proxy.test.ts` ×6, `modules/bootnode/handler.test.ts` ×23 [TDD, written before the implementation — RED confirmed via "Cannot find module" before `handler.ts` existed, GREEN on first implementation pass, no fix iteration needed], `modules/bootnode/routes.test.ts` ×7, `modules/activity/routes.test.ts` ×12, `app.test.ts` ×2). `pnpm build` (root) — clean. `pnpm -r test` (root, all packages) also run: `@ctd/sdk`'s `disclosure.mjs` test fails on a PRE-EXISTING, UNRELATED issue (missing `packages/disclosure/artifacts/disclose_recipient.json` — a circuit build artifact never generated in this environment; reproduced identically on a clean `main` via `git stash`, confirming it predates this task and is out of scope).
  - **Live**: seeded a local docker Postgres with the committed SPP backfill fixture (518 rows, via `spp-backfill-loader.ts`'s real `loadBackfillEvents`/`initSppCursor`) and the committed CT-events fixture (13 rows, normalized into 10 `ct_activity` rows via the real `normalizeCtEvent`), then ran `DATABASE_URL=... PORT=3801 pnpm --filter @grantfox/api exec tsx src/server.ts` and `curl`'d every documented case:
    - `GET /health` → `{"latest_synced_ledger":3900654}`.
    - `GET /contracts/CBTEJFLW.../events?limit=2` (the CT token) → real 2-event page, `latestLedger`/base64 `cursor` present, `topic`/`value` as base64-XDR strings.
    - `GET /accounts/CB3I3Y5.../activity` → the real `register` `ct_activity` row for that account.
    - `POST /rpc getLatestLedger` → `{"id":"857bf0...","protocolVersion":27,"sequence":3915108}` (live testnet tip, proxied).
    - `POST /rpc getEvents` with the SDK's EXACT 4-contract filter set + `startLedger:3773948` (the pool's deployment ledger) → real backfilled SPP events (`new_nullifier_event`/`new_commitment_event` topics, base64-XDR), `oldestLedger:3773975`, `latestLedger` = the SAME live tip `getLatestLedger` just reported.
    - `POST /rpc getEvents` with `startLedger` past our indexed tip → `-32002` `{"reason":"retention_threshold","fromLedger":3898970}` (our indexed max + 1).
    - `POST /rpc getEvents` with a wrong (1-contract) filter set → `-32602 "unsupported filters"`.
    - `POST /rpc getEvents` with both `startLedger` and `pagination.cursor` → `-32602 "...not both"`.
    - `POST /rpc getEvents` with an unresolvable cursor → `-32004 "cache miss; cursor not found"`.
  - DB truncated back to empty after the run (same convention as Task 8's live entry above).
  - Full transcript, deserializer-parity citations, and self-review: task-9 report (`.superpowers/sdd/2026-07-31-privacy-wallet/task-9-report.md`, gitignored).
- **Task 11 (2026-08-01)**: `pnpm --filter @grantfox/api test` (`DATABASE_URL` set) — 173/173 pass, 0 skipped, 0 failed (unchanged count — CORS registration has no dedicated unit test, covered instead by the live curl below and the full Playwright browser e2e, both of which would fail loudly without it). `pnpm --filter @grantfox/api run build` — clean.
  - **Live**: ran the real server (`PORT=3801`) against a local docker Postgres, live-reseeded by letting `pnpm --filter @grantfox/api exec tsx src/worker.ts` re-poll the deployed CT contract's real testnet events (RPC retention still covers them) rather than reloading the committed fixture — `curl -H "Origin: http://localhost:5173" -I .../accounts/:address/activity` → `access-control-allow-origin: http://localhost:5173` present. Then drove the real `@grantfox/app` dev server (`pnpm --filter app dev`, `VITE_API_URL=http://localhost:3801`) through Playwright end-to-end (two full wallets, register → deposit → merge → transfer → merge → withdraw, both activity feeds) — full transcript in the task-11 report (`.superpowers/sdd/2026-07-31-privacy-wallet/task-11-report.md`, gitignored). DB left populated (not truncated this time — the live-generated rows are the e2e evidence for that report).
