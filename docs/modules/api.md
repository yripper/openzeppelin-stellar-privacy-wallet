# api Module

> **Living document.** Read this before modifying the module. Update it in the same change whenever the module's behavior, endpoints, files, or dependencies change.

**Source:** `api/` · **Last verified:** 2026-08-01

## Purpose

`@grantfox/api` — the Postgres-backed indexer package for the privacy
wallet. Task 5 shipped the schema, the drizzle client factory, and the
`IndexerRepo` data-access surface. Task 6 added the two event sources:
`fetchRpcEvents` (Soroban RPC `getEvents`) and `fetchBootnodeEvents` (the
SPP bootnode's cached `getEvents`, used to bootstrap history past the RPC's
retention window until it hands off back to the RPC) — both return the
same `EventsPage` shape. Task 7 adds the polling engine
(`api/src/modules/indexer/poller.ts`: `pollStream`/`pollStreams` + the two
`StreamSource` factories) and the worker process (`api/src/worker.ts`) that
drives it on an interval, with per-stream exponential backoff and isolation
and a graceful-shutdown loop. Task 8 (this update) adds the CT normalizer
(`api/src/modules/indexer/normalize-ct.ts`: `normalizeCtEvent`, pure) and
wires it into `pollStream` as a post-insert step inside the SAME
transaction as the raw-event insert, CT stream only (`ctTokenId` param) —
`worker.ts`'s CT stream state now carries `TESTNET.ct.token` so every CT
event the worker fetches also lands normalized `ct_activity` rows. No HTTP
server exists yet (Task 9).

## Structure

| File | Purpose |
|---|---|
| `api/src/db/schema.ts` | drizzle `pgTable` definitions for the four tables: `events`, `ct_activity`, `cursors`, `bootnode_pages`. Exports `*Row`/`New*Row` inferred types per table. |
| `api/src/db/client.ts` | `createDb(url): { db, pool }` — builds a `pg.Pool` + `drizzle-orm/node-postgres` handle bound to `schema`. Caller owns `pool.end()`. |
| `api/src/db/repo.ts` | `createRepo(db): IndexerRepo` — the data-access surface: batch `insertEvents` (on-conflict-do-nothing by `id`), `getCursor`/`setCursor`, `insertCtActivity`, `insertBootnodePage`/`getBootnodePage`, and `withTransaction(fn)` which re-scopes all of the above to a single Postgres transaction. |
| `api/src/lib/env.ts` | `loadEnv(source?)` — zod-validated process env: `DATABASE_URL` (required), `POLL_INTERVAL_MS` (optional, default `5000`), `RPC_URL`/`BOOTNODE_URL` (optional, default to `TESTNET.rpcUrl`/`TESTNET.spp.nethermindBootnode` — must stay UNSET to take the default; an empty string fails validation, see Gotchas). |
| `api/src/index.ts` | Public package entry point; re-exports the Task 5 surface (`createDb`/`createRepo`/schema/`loadEnv`). Does NOT re-export the Task 7 poller/worker — nothing outside `api/` consumes them yet (worker.ts is a standalone entrypoint, not a library import). |
| `api/src/modules/indexer/poller.ts` | The polling engine. `pollStream(source, repo, streamKey, ctTokenId?): Promise<void>` — one page per call: fetch (network I/O, outside any transaction), then `repo.withTransaction` batch-inserts events, then (Task 8, only when `ctTokenId` is passed) runs each fetched event through `normalizeCtEvent` and `insertCtActivity`s the resulting rows — all as one post-insert step INSIDE the same transaction — then advances the cursor, all as one unit. `pollStreams(streams, repo, shouldStop?)` — runs `pollStream` per stream (forwarding each `StreamConfig`'s optional `ctTokenId`) with per-stream try/catch isolation, returning `{streamKey, ok, error?}[]`; `shouldStop` (optional) is checked between iterations (never mid-flight) so a shutdown signal can skip streams not yet started (review fix, see Gotchas). `StreamSource` — the fetch abstraction (`fetchPage(cursor): Promise<{events, nextCursor}>`); `makeRpcSource(config)` (pure-RPC, the CT stream) and `makeBootnodeThenRpcSource(config)` (bootnode-until-handoff-then-RPC, the SPP stream) are the two concrete factories, both taking an optional `timeoutMs` (review fix) and each injectable with a fake `fetchEvents`/`fetchBootnode` for tests. Owns the cursor-value encoding (`ledger:<N>`, `handoff:<N>`, `rpc:<...>`) documented in the file's module doc comment. |
| `api/src/modules/indexer/poller.test.ts` | TDD spec for the five invariants (batch+cursor atomicity; cursor advances past a filtered page; resume-from-cursor with no duplicate `events.id`; bootnode handoff → RPC switch, restart-safe; per-stream error isolation) plus `makeRpcSource` unit cases, a `pollStreams`/`shouldStop` early-exit case, and (Task 8) a `describe` block proving the CT-normalization wiring: passing `ctTokenId` normalizes fetched events into `ct_activity` rows in the SAME transaction; a non-CT-activity event still inserts its raw row but no `ct_activity` row; omitting `ctTokenId` (the SPP call site) never touches `ct_activity` even for a normalizable event; a mid-transaction crash rolls back the `ct_activity` insert too, not just the cursor. Offline via an in-memory `FakeRepo` with REAL stage/commit/rollback transaction semantics (not a stub) for invariant 1's and the CT-wiring rollback case's proof; two `describe.skipIf(!DATABASE_URL)` blocks re-prove invariant 1 and the CT wiring against real Postgres. |
| `api/src/modules/indexer/normalize-ct.ts` | The CT normalizer (Task 8). `normalizeCtEvent(raw: RawEvent, tokenId: string): CtActivityInsert[] \| null` — pure (no I/O, no `Date.now()`): returns `null` if `raw.contractId !== tokenId`, if `raw.topic` is empty, or if `topic[0]`'s symbol isn't one of `register`/`deposit`/`merge`/`withdraw`/`transfer`. Otherwise decodes the topics/value XDR (`xdr.ScVal.fromXDR` + `Address.fromScVal`/`scValToNative`/`.bytes()`) and maps to `ct_activity` row(s): `register`/`merge` → one self-row, no amount, empty `ciphertexts`; `deposit` → a row per side (collapsed to one when `from === to`), public `amount`, empty `ciphertexts`; `withdraw` → one row for `from` only, public `amount`, `ciphertexts` = the 4 checkpoint fields (`rE`/`sigma`/`bTilde`/`bAudS`); `transfer` → a row per side, `amount: null`, `ciphertexts` = the full 8-field set (adds `vTilde`/`vAudR`/`rAudR`/`vAudS`). Ciphertext values are `0x`-prefixed lowercase hex of the RAW on-chain `BytesN` bytes — not decoded through any curve-point math (see Gotchas: field names). `CtActivityInsert` is an exported alias for `NewCtActivityRow`. |
| `api/src/modules/indexer/normalize-ct.test.ts` | TDD spec against 13 REAL events fetched live from the deployed CT contract (`__fixtures__/ct-events.json`, committed; provenance and decode-transcript pointer in the file's module doc). Covers: non-CT-activity topics (the 4 constructor-setter events) → `null`; contractId mismatch → `null`; empty `topic` → `null`; a CT-emitted-but-unmapped event (`spender_transfer`, XDR round-trip-verified, not hand-encoded) → `null`; register (2 real events, one C-address one G-address account); deposit (self-deposit → 1 row; a synthesized cross-account variant → 2 rows, same amount); merge; transfer (2 rows, `amount: null`, all 8 hex ciphertext fields asserted against hand-decoded values); withdraw (1 row, public amount, 4 hex ciphertext fields); purity (no input mutation; deterministic same-input-twice). |
| `api/src/modules/indexer/__fixtures__/ct-events.json` | Committed fixture: 13 real events from the deployed CT contract `CBTEJFLW25UXIDAIWJ3KUJGI5CE2YLHM5GQM2VFU7JQZS53HE3HKGCLH` (ledger 3900254–3900654), `RawEvent[]` JSON (dates as ISO strings, restored to `Date` by each consuming test's `loadFixtures`/`loadCtFixtures` helper). Shared by `normalize-ct.test.ts`, `poller.test.ts`'s CT-wiring cases, and `worker.test.ts`'s CT-forwarding case — one capture, three test files, so they can never disagree about what a real CT event looks like. |
| `api/src/worker.ts` | The worker process. `backoffDelayMs(consecutiveFailures)` — `1s * 2^(n-1)`, capped at 60s. `StreamRuntimeState`/`buildInitialStreamState(streamKey, source, ctTokenId?)` — per-stream `{consecutiveFailures, nextAttemptAt, ctTokenId?}` bookkeeping; `ctTokenId` (Task 8) is forwarded verbatim to `pollStreams`' `StreamConfig`. `tick(states, repo, now?, shouldStop?)` — polls every stream whose `nextAttemptAt <= now` via `pollStreams` (forwarding `shouldStop` and each state's `ctTokenId`), updates backoff state per outcome. `main()` — process entrypoint: `loadEnv`, `createDb`/`createRepo`, builds the CT (`makeRpcSource`, `ctTokenId: TESTNET.ct.token`) and SPP (`makeBootnodeThenRpcSource`, no `ctTokenId`) stream states, loops `tick` on `POLL_INTERVAL_MS` (passing `() => shuttingDown` as `shouldStop`) with `Promise.race` early-wake on SIGINT/SIGTERM, `pool.end()` on exit. Guarded by an ESM `import.meta.url === pathToFileURL(process.argv[1]).href` check so `main()` only auto-runs when this file is the process entrypoint (not when imported by tests). |
| `api/src/worker.test.ts` | Unit tests for `backoffDelayMs`'s table of inputs, `tick`'s scheduling (a backing-off stream is skipped until its window elapses; a healthy stream is unaffected; a recovered stream's backoff resets to zero), that `tick` forwards `shouldStop` to `pollStreams`, and (Task 8) that `tick` forwards a stream's `ctTokenId` through to `pollStream`'s CT normalization — a real register-event fixture fed to BOTH a CT-tagged and a plain SPP-tagged `StreamRuntimeState` in the same `tick` call produces a `ct_activity` row for only the CT one. Does not exercise `main()` — process lifecycle (signals, `pool.end()`) was verified by the bounded live run instead (see Testing). |
| `api/drizzle.config.ts` | `drizzle-kit` config — schema path, migrations `out: ./drizzle`, `dialect: postgresql`, reads `DATABASE_URL` (falls back to the local docker default). |
| `api/drizzle/0000_unusual_captain_marvel.sql` | First (and, as of this task, only) migration — `CREATE TABLE` for all four tables. |
| `api/src/db/schema.test.ts` | Offline: asserts each table's column names/types/PK against this doc (drizzle `getTableColumns`/`getTableName`), plus an `events.id` format check. |
| `api/src/lib/env.test.ts` | Offline: `loadEnv` required/default/coercion/failure cases. |
| `api/src/db/repo.test.ts` | Integration, `describe.skipIf(!process.env.DATABASE_URL)`: round-trips every `IndexerRepo` method against the local docker Postgres, including transaction commit/rollback. |
| `api/src/lib/soroban-events.ts` | `fetchRpcEvents(rpcUrl, {contractIds, startLedger?, cursor?, limit, timeoutMs?}): Promise<EventsPage>` — wraps `rpc.Server.getEvents` (sdk-16); `timeoutMs` (default `DEFAULT_FETCH_TIMEOUT_MS` = 30s, also exported) bounds the call via `server.httpClient.defaults.timeout` (review fix — see module doc for why the `RpcServer` constructor's own `timeout` option does NOT work in sdk-16.2.0). Also exports `EventsPage`, `RawEvent`, `naturalEventId`, `eventIndexFromId` (shared with `bootnode-client.ts`). Pure I/O adapter: no DB access, no retry/backoff. |
| `api/src/lib/bootnode-client.ts` | `fetchBootnodeEvents(url, {contractIds, startLedger?, cursor?, timeoutMs?}): Promise<EventsPage \| {handoff: {fromLedger: number}}>` — JSON-RPC 2.0 POST to the SPP bootnode's `getEvents`. Maps `-32002` (retention handoff) to a `{handoff}` return value, throws `BootnodeCacheMissError` on `-32004`. `timeoutMs` (default `DEFAULT_FETCH_TIMEOUT_MS`, imported from `soroban-events.ts`) bounds the `fetch` call via a plain `AbortController`+`setTimeout` (review fix). Pure I/O adapter, `fetch`-based, mocked in tests. |
| `api/src/lib/soroban-events.test.ts` | Offline: mocks `rpc.Server.prototype.getEvents` (`vi.spyOn`); valid-page mapping, cursor-vs-startLedger request shape, missing-`startLedger`/`cursor` guard, missing-`contractId` mapping error, `eventIndexFromId`/`naturalEventId` unit cases, and a `timeoutMs`-wiring case (captures `this.httpClient.defaults.timeout` inside a `mockImplementation`). |
| `api/src/lib/bootnode-client.test.ts` | Offline: mocks global `fetch` (`vi.stubGlobal`); wire-shape (`pagination.cursor`, `deny_unknown_fields`-safe body) assertions, valid-page mapping, `-32002` handoff mapping, both `-32004` message variants, an unmapped JSON-RPC error code, missing-required-field mapping error, missing-`startLedger`/`cursor` guard, and a timeout case (a `fetch` mock that only ever settles via the passed `AbortSignal`, asserting rejection within a 20ms configured timeout). |

## Endpoints / Public surface

- `createDb(url: string): { db: Db; pool: Pool }` — `api/src/db/client.ts`.
- `createRepo(db: Db): IndexerRepo` — `api/src/db/repo.ts`.
- `IndexerRepo` / `RepoOps` — `api/src/db/repo.ts`. `RepoOps`: `insertEvents(rows)`, `getCursor(key)`, `setCursor(key, value)`, `insertCtActivity(rows)`, `insertBootnodePage(row)`, `getBootnodePage(cursorIn)`. `IndexerRepo` adds `withTransaction(fn)`.
- `loadEnv(source?): Env` — `api/src/lib/env.ts`. `Env = { DATABASE_URL: string; POLL_INTERVAL_MS: number; RPC_URL: string; BOOTNODE_URL: string }`.
- Tables + inferred row types (`events`/`EventRow`/`NewEventRow`, `ctActivity`/`CtActivityRow`/`NewCtActivityRow`, `cursors`/`CursorRow`/`NewCursorRow`, `bootnodePages`/`BootnodePageRow`/`NewBootnodePageRow`) — `api/src/db/schema.ts`.
- `fetchRpcEvents(rpcUrl, opts): Promise<EventsPage>`, `fetchBootnodeEvents(url, opts): Promise<EventsPage | {handoff: {fromLedger: number}}>`, `EventsPage`, `RawEvent`, `naturalEventId`, `eventIndexFromId`, `BootnodeCacheMissError`, `DEFAULT_FETCH_TIMEOUT_MS` — `api/src/lib/soroban-events.ts`, `api/src/lib/bootnode-client.ts`.
- `pollStream(source, repo, streamKey, ctTokenId?): Promise<void>`, `pollStreams(streams, repo, shouldStop?): Promise<StreamPollOutcome[]>`, `StreamSource`, `StreamConfig` (`{streamKey, source, ctTokenId?}`), `makeRpcSource(config)`, `makeBootnodeThenRpcSource(config)` — `api/src/modules/indexer/poller.ts`. Not re-exported from `api/src/index.ts` (only `worker.ts` consumes them so far).
- `normalizeCtEvent(raw: RawEvent, tokenId: string): CtActivityInsert[] | null`, `CtActivityInsert` (alias for `NewCtActivityRow`) — `api/src/modules/indexer/normalize-ct.ts`. Pure; not re-exported from `api/src/index.ts` (consumed directly by `poller.ts`).
- `main(): Promise<void>`, `tick(states, repo, now?, shouldStop?)`, `backoffDelayMs(n)`, `buildInitialStreamState(streamKey, source, ctTokenId?)` — `api/src/worker.ts`. Run via `pnpm --filter @grantfox/api worker` (dev, `tsx`) or `node dist/worker.js` (built).

## Key methods

- `createDb` — `api/src/db/client.ts:11`.
- `createRepo` / `buildRepoOps` — `api/src/db/repo.ts:83`, `api/src/db/repo.ts:39`.
- `loadEnv` — `api/src/lib/env.ts:23`.
- `fetchRpcEvents` — `api/src/lib/soroban-events.ts:171`.
- `fetchBootnodeEvents` — `api/src/lib/bootnode-client.ts:150`.
- `pollStream` / `pollStreams` — `api/src/modules/indexer/poller.ts:103`, `api/src/modules/indexer/poller.ts:152`.
- `makeRpcSource` / `makeBootnodeThenRpcSource` — `api/src/modules/indexer/poller.ts:209`, `api/src/modules/indexer/poller.ts:248`.
- `normalizeCtEvent` — `api/src/modules/indexer/normalize-ct.ts:110`.
- `main` / `tick` / `backoffDelayMs` / `buildInitialStreamState` — `api/src/worker.ts:132`, `api/src/worker.ts:67`, `api/src/worker.ts:29`, `api/src/worker.ts:44`.

## Dependencies

- `drizzle-orm` (`^0.45`, `drizzle-orm/node-postgres`) + `pg` (`^8`) — Postgres access.
- `drizzle-kit` (dev, `^0.31`) — migration generation (`db:generate`) and apply (`db:migrate`); config in `api/drizzle.config.ts`.
- `zod` (`^3.24`) — env validation.
- `@grantfox/shared` (`workspace:*`) — `TESTNET` config: `ct.token`/`ct.deployedAtLedger` (CT stream), `spp.pool`/`spp.publicKeyRegistry`/`spp.deploymentLedger`/`spp.nethermindBootnode` (SPP stream), `rpcUrl`/`spp.nethermindBootnode` as `env.ts`'s `RPC_URL`/`BOOTNODE_URL` defaults. Used by `env.ts` and `worker.ts` as of Task 7 (previously only declared).
- `@stellar/stellar-sdk` (pinned `16.2.0`) — `rpc`/`xdr` types, used by `soroban-events.ts`; `xdr`/`Address`/`scValToNative`, used by `normalize-ct.ts` (Task 8) to decode topics/value XDR into native addresses/amounts and raw ciphertext bytes.
- `fastify` (`^5`) — declared per the task-5 brief for the API server (Task 9); still unused.
- `tsx` (dev, `^4.19.0`) — runs `worker.ts` directly in dev (`pnpm --filter @grantfox/api worker`) without a build step.
- Local Postgres: repo-root `docker-compose.yml` (`postgres:16-alpine`, user/pass/db `grantfox`, host port `${DB_HOST_PORT:-5433}`). `DATABASE_URL` default: `postgres://grantfox:grantfox@localhost:5433/grantfox` — set in repo-root `.env` and `.env.example`.
- `events.id` format (`${ledger}-${txHash}-${opIndex}-${eventIndex}`) matches `@ctd/sdk`'s `naturalEventId` (`packages/ctd-sdk/src/chain/events.ts:236`). `api/src/lib/soroban-events.ts` exports its own `naturalEventId`/`eventIndexFromId` that REPLICATE (not import) that logic — `@ctd/sdk` pulls in the zk-proving stack (`@aztec/bb.js`, `@noir-lang/noir_js`), which `@grantfox/api` has no other reason to depend on. `poller.ts` constructs `events` rows straight from `RawEvent` (which already carries the correctly-formatted `id`), not a third reimplementation.

## Gotchas & invariants

- `insertEvents` is on-conflict-do-nothing **by `id` only** — a re-inserted row with the same id but different payload is silently ignored (the first write wins). This is intentional for idempotent worker restarts, but means a genuine data-correction requires a manual `UPDATE`, not a re-insert.
- `insertBootnodePage` is also on-conflict-do-nothing by `cursorIn`, same idempotency rationale — a page fetch for an already-cached `cursorIn` is a no-op even if `response` would differ.
- `setCursor` is a real upsert (`onConflictDoUpdate`), unlike the two above — cursors are meant to move forward and always reflect the latest value.
- `withTransaction`'s callback receives a `RepoOps` bound to the transaction handle (not the outer `IndexerRepo`), so it can call any insert/query helper but not nest another `withTransaction`.
- `ct_activity.amount` is the only nullable column in the schema; everything else (including `bootnode_pages.cursor_out` / `last_event_ledger`) is `NOT NULL` per the task-5 brief's literal column list — there was no signal to make anything else optional.
- `cursors.updated_at` and `bootnode_pages.created_at` default to `now()` at the DB level (`defaultNow()`), not application code — this wasn't spelled out in the brief but matches the "troqpay shape" convention for timestamped audit tables.
- `api/tsconfig.json` excludes `src/**/*.test.ts` from the `build` output — test files are type-checked by `vitest` (esbuild transpile, not full `tsc`) and separately by a full-tree `tsc --noEmit` during review, not by `pnpm build`.
- `fetchRpcEvents`/`fetchBootnodeEvents` both derive `RawEvent.id` as `${ledger}-${txHash}-${opIndex}-${eventIndex}`; `opIndex`/`txIndex` come directly off sdk-16's `operationIndex`/`transactionIndex` fields (no toid bit-masking needed, unlike `@ctd/sdk`'s older-SDK-targeting `rpcEventCoords`), but `eventIndex` still has no dedicated field on either source and is parsed from the second segment of the event `id` (`<toid>-<eventOrder>`) by the shared `eventIndexFromId` helper.
- `RawEvent.topic`/`valueXdr` are base64-encoded XDR strings for BOTH sources, not decoded native values — `fetchRpcEvents` re-encodes the SDK's parsed `xdr.ScVal`s via `.toXDR("base64")`; `fetchBootnodeEvents` passes the bootnode's already-base64 wire strings straight through. Decoding to CT-specific native values (event names, addresses, amounts) is left to the normalizer (Task 8), not done here.
- `fetchRpcEvents` throws if an `EventResponse.contractId` is `undefined` — it does NOT fall back to a single "requested" contract id (unlike troqpay's `soroban-gateway.ts`, whose `getContractEvents` fell back to its one request-scoped `contractId`), because this adapter filters by potentially MULTIPLE `contractIds` and a wrong fallback would silently mislabel a row's contract.
- `fetchBootnodeEvents` throws if a mapped event is missing `operationIndex`/`transactionIndex`/`txHash`/`inSuccessfulContractCall` (all `Option` on the bootnode's Rust `Event` struct) — the `events` table requires them `NOT NULL` and the id can't be built without `txHash`/`operationIndex`, so this adapter fails fast rather than guessing.
- The bootnode's wire params nest `cursor`/`limit` under `params.pagination`, NOT flat on `params` — `GetEventsParams` is `#[serde(deny_unknown_fields)]` server-side, so a flat body is rejected. Neither `soroban-events.ts` nor `bootnode-client.ts` retries or logs; `poller.ts`/`worker.ts` own all retry/backoff policy (including on `BootnodeCacheMissError` — an ordinary caught error, backed off like any other stream failure, no special-casing needed).
- **Both network adapters are timeout-bounded, `DEFAULT_FETCH_TIMEOUT_MS` = 30s, injectable per-call as `timeoutMs`** (Task 7 review fix — before this, a hung fetch on one stream blocked `pollStreams`' sequential loop indefinitely, and since `worker.ts`'s `while` loop only checks its shutdown flag BETWEEN `tick()` calls, a hang meant SIGTERM never produced the documented clean exit). `fetchBootnodeEvents` uses a plain `AbortController`+`setTimeout` (its own `fetch` call). `fetchRpcEvents` is the more interesting one: `RpcServer.Options.timeout` — the CONSTRUCTOR option `server.d.ts` documents — is a **dead type in the installed sdk-16.2.0 build**; `new RpcServer(url, opts)`'s actual (transpiled) constructor only ever reads `opts.headers`/`opts.allowHttp`, never `opts.timeout` (verified by reading `rpc/server.js`, not trusting the `.d.ts`). The mechanism that DOES work: `server.httpClient.defaults.timeout = ms`, mutating the mutable `HttpClientDefaults` object the SDK's own doc-comment example shows is a supported pattern (`server.httpClient.defaults.headers[...] = ...`) — `getEvents`'s call path merges `httpClient.defaults` into every request and (since no `maxRedirects`/`maxContentLength` is set) routes through the `feaxios` dependency's `instance.request`, which genuinely honors `config.timeout` via `AbortSignal.timeout` and rejects with a plain `AxiosError` on expiry. Covered by a "wiring" test in `soroban-events.test.ts` (captures `this.httpClient.defaults.timeout` inside a `getEvents` mock) rather than a live-hang test, since the SDK's `getEvents` is normally mocked wholesale in tests (`vi.spyOn(rpc.Server.prototype, "getEvents")`), bypassing the HTTP layer entirely.
- `pollStreams(streams, repo, shouldStop?)`'s `shouldStop` is checked only BETWEEN streams, never mid-flight — a stream already in progress when a shutdown signal arrives always runs to completion or its own timeout. This bounds `tick()`'s shutdown-triggered worst case to "at most one stream's timeout" rather than "every remaining stream's timeout summed," without restructuring the sequential for-loop into concurrent execution. `worker.ts`'s `main()` passes `() => shuttingDown` as `tick`'s `shouldStop`.
- **Cursor-value encoding is meaningful, not opaque, for the two `poller.ts` source factories** — see the file's module doc comment for the full state machine. Short version: `null` = start from configured `startLedger`; a bare string = an opaque cursor from the underlying source passed straight back; `ledger:<N>` = a synthesized fallback when a page reports `cursor: null` (EventsPage's "no further cursor" case — neither `fetchRpcEvents` nor `fetchBootnodeEvents` accepts a bare ledger number as `cursor`, so this marker is decoded back into a `startLedger` fetch, never sent to the network as-is); `handoff:<N>` (SPP only) = the literal transitional marker the task brief asks for, persisted the instant the bootnode reports a retention handoff; `rpc:<...>` (SPP only) = wraps EVERY subsequent cursor once handed off, which is what makes the handoff survive a worker restart — `makeBootnodeThenRpcSource` decides "bootnode or RPC?" purely by reading this prefix off the persisted string, never from in-memory state.
- `pollStream` reads the cursor and calls `source.fetchPage` (network I/O) BEFORE opening the transaction — a transaction is never held open across a network call. Only the insert + `setCursor` happen inside `repo.withTransaction`.
- A page with `nextCursor !== null` always advances the cursor even when `events` is empty (invariant 2) — `insertEvents([])` is `RepoOps`'s documented no-op, `setCursor` still runs. `pollStream` only skips `setCursor` if a `StreamSource` ever returns a literal `null` `nextCursor`, which neither shipped factory does (they always synthesize `ledger:`/`handoff:`/`rpc:` instead) — this is a defensive branch, not exercised by the shipped sources.
- **`api/package.json`'s `test` script runs `vitest run --no-file-parallelism`** (Task 7) — vitest's default is to run test FILES concurrently, and with two DB-integration test files now sharing the same live local-Postgres tables (`repo.test.ts` and `poller.test.ts`'s `describe.skipIf(!DATABASE_URL)` block, both `beforeEach`-truncating `events`/`ct_activity`/`cursors`/`bootnode_pages`), concurrent files raced: one file's truncate could fire mid-assertion in the other, observed as a rare, non-deterministic `ct_activity` row-count failure in `repo.test.ts` (not caused by a bug in either file's own logic — confirmed reproducible only under file-level parallelism, gone after 10/10 clean runs with the flag). Any FUTURE integration test file added to this package should assume it may run interleaved with these two unless this flag stays in place.
- **`normalize-ct.ts`'s on-chain field names follow the FIXTURES, not the checked-out `resources/stellar-contracts` Rust source (Task 8, verified 2026-08-01).** The deployed testnet CT contract's real event data-map field names (`r_e`, `sigma`, `b_tilde`, `b_aud_s`, `v_tilde`, `v_aud_r`, `r_aud_r`, `v_aud_s` — decoded straight from `__fixtures__/ct-events.json`'s `valueXdr`) match the vendored `@ctd/sdk`'s `packages/ctd-sdk/src/chain/events.ts` `buildConfidentialEvent` mapping EXACTLY (event names, topic layout, and both the snake_case on-chain names and their camelCase row-field names `rE`/`vTilde`/`sigma`/`bTilde`/`vAudR`/`rAudR`/`vAudS`/`bAudS` — cross-checked line-by-line against `events.ts:167-193`). They do NOT match the branch checked out at `resources/stellar-contracts` (`feat/confidential-verifier-ultrahonk`, `packages/tokens/src/confidential/mod.rs`), which has since renamed several fields (e.g. `b_aud_s` → `b_tilde_aud_s`) — the deployed contract predates that rename. If `resources/stellar-contracts` is ever updated to a build matching the rename, `normalize-ct.ts` and its fixtures need re-verifying against a fresh live capture before trusting the Rust source over the fixtures again.
- `normalize-ct.ts`'s `ciphertexts` values are `0x`-prefixed lowercase hex of the RAW on-chain `BytesN<32>`/`BytesN<64>` bytes (`val.bytes()`, no curve-point decode) — a deliberate choice to avoid depending on `@ctd/sdk`'s crypto helpers (`pointFromBytes`/`fromBytesBE`, which pull in the zk-proving stack `@aztec/bb.js`/`@noir-lang/noir_js`, same rationale as the `events.id` gotcha above). The wallet client (Task 11) decodes these hex strings itself with the same byte layout those helpers expect — this is why `withdraw`'s `rE` (a `Point`, 64 bytes) and `sigma`/`bTilde`/`bAudS` (field elements, 32 bytes) can share one `hexBytes()` helper: the normalizer never needs to know which is which, only the wallet does.
- **`withdraw` produces a row for `from` ONLY, not `to`** (task-8-brief.md's literal wording) — `to` merely receives plain SEP-41 tokens leaving the confidential system and isn't a CT-tracked account for this event. `deposit` and `transfer` both collapse to a SINGLE row (not two identical ones) when `from === to` — a self-deposit/self-transfer is a real on-chain pattern in the fixtures (e.g. fixture 8, ledger 3900642) and emitting two identical rows would double-count that one action in that one account's activity feed.
- `normalizeCtEvent` returns `null` (not an empty array) for anything it doesn't map: wrong `contractId`, zero topics, a non-symbol `topic[0]`, or a real CT-emitted event outside its five-name whitelist (`spender_transfer`/`set_spender`/`revoke_spender`/the compliance events/the four constructor-setter events seen live in the fixtures). `pollStream` treats both `null` and `[]` identically via `?? []` before `flatMap`, so this distinction only matters to `normalizeCtEvent`'s own callers/tests, not to the wiring.
- **Live-verification blocker (Task 7, 2026-07-31): Nethermind's public bootnode (`https://bootnode.dev-nethermind.xyz`) rejects every `getEvents` request with `-32602 unsupported filters`** — verified via direct `curl`, not just through our client: the canonical SPP testnet `pool`/`publicKeyRegistry` ids (matching BOTH `packages/shared/src/config.ts` AND the vendored reference repo's own `resources/stellar-private-payments/deployments/testnet/deployments.json`), a garbage contract id, and empty `filters` all get the identical error. `tools/bootnode/src/rpc.rs:78`'s `is_allowed_filters` check rejects any request whose `contractIds` don't exactly match the bootnode's own configured allow-list — so this is an operational/deployment mismatch on the PUBLIC bootnode's side (or it's scoped to a different contract set than this repo's vendored copy), not a wire-shape bug in `bootnode-client.ts` (confirmed byte-for-byte against `messages.rs`'s own `for_contracts` test helper). `getLatestLedger` against the same bootnode succeeds, so the service itself is up. Compounding this: the SPP pool's `deploymentLedger` (3773948) is already outside the live RPC's retention window (currently ledger ~3780722+ only) — so right now there is NO live path (bootnode blocked, RPC out of retention) to backfill SPP history from deployment. The RPC-after-handoff leg itself was verified structurally live (see Testing) — it is the bootnode's contract allow-list that blocks the SPP stream end-to-end today. Re-check by re-running the two `curl` probes in the task-7 report if picking this back up.

## Testing

- `pnpm --filter @grantfox/api test` (`vitest run --no-file-parallelism`) — offline: 72 tests (`env.test.ts` ×7, `schema.test.ts` ×5, `soroban-events.test.ts` ×9, `bootnode-client.test.ts` ×10, `poller.test.ts` ×12 of 15, `worker.test.ts` ×13, `normalize-ct.test.ts` ×16 [Task 8]) always run; `repo.test.ts` ×6 and `poller.test.ts`'s 3 `describe.skipIf(!DATABASE_URL)` integration cases (the original invariant-1 proof plus Task 8's two CT-wiring cases) auto-skip when `DATABASE_URL` is unset (9 tests skipped total, 81 tests overall).
- Integration: `docker compose up -d postgres` (repo root), then `DATABASE_URL=postgres://grantfox:grantfox@localhost:5433/grantfox pnpm --filter @grantfox/api test` — all 81 tests pass, including `repo.test.ts`'s transaction commit/rollback, `poller.test.ts`'s real-Postgres invariant-1 proof (a NOT NULL violation mid-`pollStream` rolls back both the insert and the cursor), the offline in-memory-`FakeRepo` invariant-1 proof (stage/commit/rollback semantics, not a stub), and (Task 8) `poller.test.ts`'s real-Postgres CT-wiring proofs: a `register` event lands both its `events` row and its normalized `ct_activity` row in ONE transaction, and a mid-transaction failure (a bad second event) rolls back BOTH tables together, not just the cursor.
- `soroban-events.test.ts`/`bootnode-client.test.ts` mock `rpc.Server.prototype.getEvents`/global `fetch` respectively — neither hits a live network endpoint. `poller.test.ts`/`worker.test.ts`/`normalize-ct.test.ts` are fully offline (fake `StreamSource`s / injected `fetchEvents`/`fetchBootnode` / committed fixture JSON), except the real-Postgres cases above.
- Migration apply: `docker compose up -d postgres` then `DATABASE_URL=... pnpm --filter @grantfox/api exec drizzle-kit migrate` — verified applying `0000_unusual_captain_marvel.sql` cleanly to a fresh volume, `\dt`/`\d <table>` column-by-column matches the brief.
- Live (Task 7, testnet): `DATABASE_URL=... POLL_INTERVAL_MS=3000 pnpm --filter @grantfox/api worker` — CT stream landed 13 real rows in `events` (ledger 3900254–3900654, our smoke-test transactions) and its cursor round-tripped correctly on a second run (still 13 rows — no duplicates, live invariant-3 evidence). SPP stream hit the bootnode blocker above on every tick without affecting the CT stream (live invariant-5 evidence). The RPC-after-handoff leg was verified structurally by calling `pollStream`+`makeRpcSource` against the real DB for the SPP contract ids from a within-retention ledger (mechanism works: cursor persists; 0 events, because there is genuinely no on-chain activity for these contracts anywhere in the RPC's current ~120k-ledger retention window — confirmed by scanning it end to end). SIGINT/SIGTERM handling was verified against the BUILT artifact (`node dist/worker.js`, a single process — the shape a real deployment would run) via direct `kill`: both signals produce `[worker] received SIG..., shutting down after the current tick` → `[worker] shut down cleanly` and a clean process exit. (A `kill -TERM` sent to only the outer PID of a `pnpm`/`npx tsx` dev invocation does NOT reach the actual runtime process — those wrap 2–3 levels of child processes; a real terminal's Ctrl+C and a production single-process deploy both avoid this, but scripted single-PID signal tests against the dev command are misleading. Test against `node dist/worker.js` instead.) Full details, including the exact `curl` probes: task-7 report (`.superpowers/sdd/2026-07-31-privacy-wallet/task-7-report.md`, gitignored).
- Live (Task 8, testnet, 2026-08-01): `DATABASE_URL=... timeout 25s pnpm --filter @grantfox/api worker` against the same real CT contract — the 13 live events from the CT stream landed both their `events` rows AND normalized `ct_activity` rows (10 rows: 4 `register` + 1 self-`deposit` + 2 `merge` + 2 `transfer` + 1 `withdraw` — the 4 constructor-setter events correctly produced zero `ct_activity` rows). Spot-checked one `transfer` row's `ciphertexts` jsonb in Postgres directly: all 8 hex fields present and byte-identical to `normalize-ct.test.ts`'s hand-decoded expectations. SPP stream failed on the known dead-bootnode issue (unrelated to this task, tracked by Task 8.5) without affecting the CT stream — confirms per-stream isolation still holds with the new wiring. DB truncated back to empty after this run.
- `pnpm --filter @grantfox/api build` (`tsc -p tsconfig.json`) — verified clean.
