{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "node scripts/fetch-spp-dist.mjs && pnpm install --frozen-lockfile && pnpm --filter @privacy-wallet/shared build && pnpm --filter @ctd/sdk build && pnpm --filter app build"
  },
  "deploy": {
    "startCommand": "node app/server.mjs",
    "numReplicas": 1
  }
}
