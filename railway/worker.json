{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @privacy-wallet/shared build && pnpm --filter @ctd/sdk build && pnpm --filter @privacy-wallet/api build"
  },
  "deploy": {
    "startCommand": "node api/dist/worker.js",
    "numReplicas": 1
  }
}
