# Distribution licensing materials

This `deployments/legal/` directory contains **distribution-only** licensing texts and notice templates that are copied into build outputs (for example `dist/`).

It **does not** change the license of the repository source code itself. Unless explicitly stated otherwise, the repository source code remains licensed under Apache-2.0 as described by the repo-root `LICENSE`.
